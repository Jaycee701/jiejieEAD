# jiejieEAD 使用手册

> ⚠️ **免责声明（务必阅读）**
> 本工具**仅限用于已获书面授权的安全测试、安全研究与密码应用合规审计**。
> 未授权对系统进行渗透、破解属于违法行为，使用者需自行承担全部法律责任。
> 本工具全流程**本地离线运算**，不会上传任何密钥、明文或密文。
> 许可协议见随包 `LICENSE`（MIT）。

---

> 本文所有命令与输出均为**实机跑通**的结果，可直接复制使用。
> 路径约定：`%APP%` = `桌面\jiejieEAD2.0.0`
> 内置解释器：`%APP%\resources\python-embed\python.exe`（无需另装 Python）
>
> **当前版本：jiejieEAD v2.0.0（密码引擎 v4）** —— 从「只支持 SM4-CBC / AES-256-CBC」扩展为
> **17 种算法 × 6 种工作模式 × 5 种填充** 的完整矩阵，且**文件扫描器识别到的组合
> 全部可在工作台 / MITM 中复现**。详见第十六节「支持矩阵」。

---

## 一、三种使用入口

| 入口 | 适合场景 | 怎么用 |
|---|---|---|
| **① 桌面 GUI** | 日常使用，推荐 | 双击 `%APP%\jiejieEAD.exe` |
| **② 中文菜单** | 不想记命令 | 双击 `桌面\CryptoSec-Toolkit\启动工具.bat` |
| **③ 纯命令行** | 写脚本 / 批量处理 | 见第十四节「命令速查」 |

> 入口 ② ③ 依赖项目目录里的 `.venv`；入口 ① 自带运行时不依赖任何外部环境。

---

## 二、GUI 界面一览（十一个 Tab）

双击 `jiejieEAD.exe` 后，顶部有十一个页签：

```
┌──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  🔒 加解密工作台 │ 🔗 MITM 自动加解密 │ 💻 国密密码分析 │ 🔍 文件扫描分析 │ 📂 文件夹整体分析 │ ✨ AI 智能分析 │ 🔌 MCP 接入 │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

> **小技巧**：拿不准该用哪个页签时，先走 **Tab 4 文件扫描分析**——它扫完会给出
> 「一键填入工作台 / 一键填入 MITM」按钮，自动帮你跳到正确页签并把密钥填好。
> 如果拿到的是**整个项目目录**、想看清文件之间怎么串起来的，用 **Tab 5 文件夹整体分析**：
> 它会建出跨文件关系图与数据流链路，并给出整体总结。
> 如果只拿到一段代码/抓包片段、不想先落盘成文件，直接用 **Tab 6 AI 智能分析**
> 粘贴进去，它会判定算法/模式/填充/密钥并一键回填（在线 LLM 为主，离线规则兜底）。
> 想把本工具的能力交给 **WorkBuddy / Claude Desktop** 这类 AI 客户端直接调用，
> 用 **Tab 7 MCP 接入**：体检 → 一键装依赖 → 握手自检 → 写入客户端配置，全程点按钮（详见第九节）。

> **窗口默认尺寸已按七个页签调整**（1180×720，最小 1040×640）。
> 若仍觉得页签挤，横向拖动窗口即可；页签栏溢出时 Element Plus 会给出左右箭头。

**先把状态栏看对**：程序启动会自动跑 `check_env`，确认内置 Python 与
`Crypto / gmssl / scapy / pyasn1` 依赖就绪。若显示"依赖检查通过"即可正常使用。

---

## 三、Tab 1「加解密工作台」怎么用

### 3.1 字段说明

| 字段 | 填什么 |
|---|---|
| **算法** | 下拉选择，共 17 种：`SM4` / `AES-128` / `AES-192` / `AES-256` / `DES` / `3DES` / `RC4` / `RC5-32/12/16` / `RC5-32/16/16` / `RC5-64/16/16` / `RC6-32/20/16` / `RC6-32/16/16` / `Blowfish` / `CAST5 (CAST-128)` / `RC2` / `ChaCha20` / `Salsa20`。其中 RC5/RC6 为自实现（已用公开向量验证），其余为 pycryptodome 原生 |
| **工作模式** | 下拉选择：`CBC` / `ECB` / `CFB` / `OFB` / `CTR` / `GCM`（RC4 只有流模式，下拉自动隐藏） |
| **填充方式** | 下拉选择：`PKCS7` / `Zero` / `None` / `ISO7816` / `ANSIX923`（流模式/流式模式自动禁用） |
| **操作** | `加密` / `解密` |
| **密钥 KEY** | HEX，长度随算法自动校验（AES-256=64 位、AES-128/SM4=32 位、DES=16 位、3DES=48 位、RC4 任意） |
| **向量 IV / Nonce** | 仅 CBC/CFB/OFB/CTR/GCM 需要。点旁边 🎲 按钮随机生成；GCM 的 IV 是 **12 字节**（24 位 HEX） |
| **输入方式** | `文本` 或 `文件`（文件支持任意二进制） |
| **密文编码** | `Base64` / `Hex` / `Raw`，加解密双向都按这个口径 |
| **结果保存到** | 留空则只在下方显示；点了会写文件 |

> **下拉框是"活"的**：程序启动时会调用 `cli_crypto.py --spec --json` 拉取密码引擎的
> **能力矩阵**，可选模式/填充/密钥长度全部由引擎实时提供——引擎加一个算法，界面自动多一项，
> 不存在"界面里能选、命令行却跑不了"的情况。

> ⚠️ KEY / IV 旁边有随机生成按钮，**别手写全 0**。工具会告警：
> `[警告] 检测到全同字节 IV（如全 0），CBC 模式 IV 应随机生成且每次不同。`

### 3.2 完整性校验：能不能"自动发现密钥错了"

这是选模式时要懂的一个点。**有些组合自带校验，密钥错了会立刻报错；有些组合没有校验，
密钥错了只会解出一堆合法但无意义的字节。**

| 组合 | 密钥错误能否自动发现 | 解密失败时的表现 |
|---|---|---|
| `GCM`（任意填充） | ✅ 能（认证标签不匹配） | 明确报 `GCM 认证失败：密钥/IV/AAD 错误或密文被篡改` |
| 分组模式 + `PKCS7` / `ISO7816` / `ANSIX923` | ✅ 能（填充字节非法） | 明确报 `去填充失败：通常说明密钥或 IV 错误` |
| `ECB` / `CBC` + `Zero` / `None` | ❌ 不能 | 不报错，但明文是乱码，**需人工判断** |
| `CFB` / `OFB` / `CTR` / `RC4` | ❌ 不能（流式加密无填充） | 同上 |

> 选到"不能自动发现"的组合时，界面上会出现一条**橙色提示条**，并且点解密会先弹确认框
> 提醒你"本组合无完整性校验，请人工核对明文"。这不是限制，是防止你把乱码当成解密结果。

### 3.3 操作步骤（以 SM4-CBC 加密为例）

1. 算法选 `SM4`，工作模式 `CBC`，填充 `PKCS7`，操作选 `加密`
2. 密钥 KEY 点 🎲 生成（或填 `0123456789abcdeffedcba9876543210`）
3. 向量 IV 点 🎲 生成
4. 输入方式选 `文本`，输入 `hello`
5. 密文编码选 `Base64`
6. 点 **执行** → 下方结果框出现密文 `2wZpvYQeVFDsFlLs1CttYA==`，点 **复制** 取走

**解密回去**：算法/模式/填充/KEY/IV 保持一致，操作切到 `解密`，把密文粘进输入框，点执行 → 得到 `hello`。

> 有个贴心的 **「交换到反向操作」** 按钮：加密完点一下，密文会自动填到解密侧，
> 不用手动复制粘贴。

### 3.4 举个例子：换成 ECB + Zero 填充

很多老系统（尤其是 Java/C# 写的）用的是 `ECB` + `ZeroPadding`，跟 `CBC+PKCS7` 的密文**完全不通用**。
现在可以直接复现：

1. 算法 `SM4`，工作模式选 `ECB`，填充选 `Zero`
2. 向量 IV **留空**（ECB 不需要 IV，界面会自动禁用该输入框）
3. 密钥填 `0123456789abcdeffedcba9876543210`，输入 `hello world`
4. 点执行 → 得到 Base64 密文

> 反过来，如果用 `CBC` 去解这段 ECB 密文，虽然可能"解开了"，但结果是乱码——
> 因为模式错了。这就是为什么**必须先把模式/填充识别对**，工具现在做到了。

---

## 四、Tab 2「MITM 自动加解密」怎么用（重点）

这是本工具最有价值的模块：**代理拦包时自动把密文解密成明文给你看，改完再自动加密回去**。

### 4.1 原理

```
浏览器/APP ──密文──> [mitmweb 代理 :8080] ──明文──> 目标服务器
                            │  ① 请求方向：解密，日志面板实时显示明文
                            │  ② 你在 Web 界面改明文包
                            ▼  ③ 响应方向：加密回注，对端收到合法密文
```

### 4.2 配置项

| 字段 | 说明 |
|---|---|
| **监听端口** | 代理端口，默认 8080 |
| **Web 端口** | 改包界面端口，默认 8081 |
| **目标域名** | **白名单**，每行一个。只处理列表内域名的流量 |
| **算法** | SM4 / AES-128 / AES-192 / AES-256 / DES / 3DES / RC4 |
| **工作模式 / 填充** | 与被测系统一致（如 `ECB`+`Zero`、`CBC`+`PKCS7`、`GCM`） |
| **KEY / IV** | 与被测系统一致的 HEX 密钥（IV 在 ECB 模式下不需要） |
| **Body 编码** | `Base64`（密文再套一层 Base64，最常见）/ `Hex` / `None`（Body 直接是二进制密文） |
| **处理方向** | 可只解请求、只解响应、或双向 |

> Tab 1 支持的算法/模式/填充，Tab 2 **全部同样支持**——两者共用同一个密码引擎
> `crypto_core.py`，不会出现"工作台能跑、MITM 跑不了"的割裂。`工作模式`/`填充` 两个字段
> 是从扫描结果一键填入的，也是手填的。

### 4.3 完整实战步骤

1. **填白名单**：目标域名里填 `api.example.com`（每行一个）
2. **填密钥**：把你从 JS 逆向 / 抓包分析得到的 KEY、IV 填进去
3. **选编码**：一般业务是"密文再 Base64"，选 `Base64`
4. 点 **启动代理**
5. 启动成功后，右侧日志面板会实时滚动，出现类似：
   ```
   Web server listening at http://127.0.0.1:8081/?token=xxxxxxxx
   ```
   界面上会自动捕获这条地址并显示在蓝色提示条里
6. 点 **打开改包界面** 或 **复制改包地址**（**注意必须带 token**，直接输不带 token 的地址进不去）
7. **配置客户端走代理**：把浏览器/手机 Wi-Fi 代理设为 `127.0.0.1:8080`
8. 此时日志面板会**实时打印解密后的明文**，例如：
   ```
   {"username":"admin","password":"P@ssw0rd","token":"abc123"}
   ```
9. 在 Web 改包界面修改明文 → 放行 → 插件自动加密后发给服务器
10. 用完点 **停止代理**（程序退出时也会自动清理残留进程）

> 如果解密失败，日志会明确提示「填充校验失败」——说明 KEY/IV 不对，**不会**静默给你一堆乱码。

---

## 五、Tab 3「国密密码分析」怎么用

顶部有三个单选按钮，对应三件事，按需切换：

| 模式 | 做什么 | 输入 |
|---|---|---|
| **SM3 摘要** | 计算数据的 SM3 哈希值（国密摘要，256 位） | 文本 或 文件 |
| **SM2 公钥审计** | 对 SM2 公钥做 9 维安全体检（曲线、点是否在曲线上、是否弱公钥等） | PEM / X.509 证书 / 裸 HEX 公钥文件 |
| **TLCP 流量解析** | 解析国密 TLS（TLCP）握手流量，识别密码套件与证书 | pcap 抓包文件 |

**用法**：选模式 → 填/选输入文件 → 点执行 → 下方结果区给出结构化结论。

> 什么时候用它：扫到 `pcap` 时用 TLCP 解析；扫到 `PEM / SM2 公钥` 时做公钥审计；
> 需要校验一段数据完整性时用 SM3。Tab 4 扫描器的「跳转分析」按钮会自动把你送到这里。

---

## 六、Tab 4「文件扫描分析」怎么用（自动化入口）

**一句话**：扔一个文件进去，工具自动识别它是什么、里面用了什么加密、**把密钥和算法抠出来**，然后一键填到工作台或 MITM 页签。

这是全自动化的「逆向预处理」环节，专治：拿到一个 JS / APK / 抓包文件，不知道密钥在哪、不知道该选哪个页签。

### 6.1 它能识别什么

| 类别 | 具体能力 |
|---|---|
| **文件类型** | 按魔数 + 内容特征自动判别：PCAP 抓包、PEM 密钥/证书、ZIP/APK/JAR、ELF、PE、JS/H5、JSON、XML/HTML、YAML、纯文本、二进制 |
| **加密算法** | SM2/SM3/SM4、AES、DES/3DES、RC4、RSA、MD5/SHA1/SHA256、Base64/Hex 编码、GCM/CBC/ECB 模式，共 20+ 条规则，分高中低置信度 |
| **密钥材料** | 变量名上下文（`key`/`iv`/`secret`/`password` 赋值）、HEX 长度特征、Base64 长度特征、二进制字符串常量池、PEM 块、SM2 裸公钥（`04`+128 位 HEX） |
| **加密库指纹** | Node crypto、CryptoJS、gmssl、BouncyCastle、OpenSSL、Web Crypto API、pycryptodome 等 |
| **内层文件** | 如果是 ZIP/APK/JAR，会**钻进包内**继续扫 JS/配置文件 |

### 6.1.1 关键：识别到什么，就能复现什么

扫描器识别出的 **算法 / 工作模式 / 填充** 三种属性，**全部**都能被工作台和 MITM 页签原样复现——
扫描结果里的 `--mode` / `--padding` 参数可以直接拿去命令行跑：

| 扫描器能识别的 | 工作台/MITM 能复现的 | 说明 |
|---|---|---|
| SM4 / AES-128 / AES-192 / AES-256 / DES / 3DES / RC4 / RC5 / RC6 / Blowfish / CAST5 / RC2 / ChaCha20 / Salsa20 | 同左，17 种全支持 | 分组 8 字节的算法（DES/3DES/RC5-32/Blowfish/CAST5/RC2）无 GCM |
| CBC / ECB / CFB / OFB / CTR / GCM | 同左，6 种全支持 | — |
| PKCS7 / Zero / ISO7816 / ANSIX923 / None | 同左，5 种全支持 | — |
| Base64 / Hex / 裸二进制 | 同左 | — |

> 扫描器内置一张**镜像能力表**，其自检会拿它和密码引擎 `crypto_core.py` 的能力矩阵逐项比对，
> 一旦两边不一致就**自检失败**。所以「扫描器说支持、实际跑不了」这种坑在架构上被堵死了。

**唯一需要你自己判断的情况**：识别的组合属于"无完整性校验"（如 `ECB`+`Zero`、`RC4`）时，
扫描报告会打印一条 `[注意]`，告诉你有多少把握、以及复现时如何人工确认。这类组合的解密
**不会报错**，所以工具主动提醒你"解出来像不像业务数据"，而不是假装成功。

### 6.2 操作步骤

1. 切到 **🔍 文件扫描分析** 页签
2. 点 **选择文件** → 挑一个待分析文件（JS 脚本 / APK / pcap / 任意二进制都行）
3. 需要钻包解析时勾上 **深度扫描**（默认开）
4. 点 **开始扫描**
5. 看结果：
   - **左栏**：提取到的密钥材料卡片（每条带 `高/中/低` 置信度标签）+ 识别到的算法标签
   - **右栏**：文件概况、**加密规则**总结（高亮块）、风险提示、包内命中文件列表
6. 拿到密钥后，选一种方式往下走：

| 按钮 | 效果 |
|---|---|
| **设为密钥 / 设为 IV** | 在密钥卡片上点，把该值单独填进工作台对应字段 |
| **一键填入工作台** | 自动切到 Tab 1，并把算法 / **工作模式** / **填充** / 密钥 / IV / 编码全部填好 |
| **一键填入 MITM** | 自动切到 Tab 2，把算法 / **工作模式** / **填充** / 密钥 / IV / Body 编码填好（省去手抄） |
| **跳转分析** | 扫描结果指向国密分析 / 加解密页签时，一键跳过去 |

> **自动分流逻辑**：扫到 `pcap` → 建议去国密分析做 TLCP 解析；
> 扫到 `PEM / SM2 公钥` → 建议去国密分析做公钥审计；
> 扫到 `SM4 / AES 密钥` → 建议去加解密工作台。

### 6.2.1 ④ AI 深度分析（v1.1 新增，右栏底部）

扫描完成后，右栏「③ 加密逻辑与风险」下方会自动出现 **④ AI 深度分析** 面板：

- 点 **AI 分析此文件** → 对当前文件跑一轮 AI 分析（在线 LLM 优先，失败自动离线兜底，
  与 Tab 6 共用同一份 LLM 配置）
- 展示：来源徽标 / 算法 / 模式 / 填充 / 编码 / 密钥 / IV / 风险提示
- 提供 **填入工作台 / 填入 MITM / 学习此规则** 三个按钮

> 它和 Tab 6 的区别：Tab 6 分析你**粘贴的文本**，这里直接分析**扫描选中的文件**。
> 两者结果互不覆盖；任一处的「一键填入」都会自动把结论沉淀进本地知识库（见 §8.5）。

### 6.3 实战示例：从 JS 里抠出 SM4-ECB + Zero 的密钥

假设某老系统 H5 页面 `app.js` 里有这么几行：

```javascript
var SM4_KEY = "0123456789abcdeffedcba9876543210";
var mode = "ECB";
var padding = "ZeroPadding";
function enc(data){
  var c = sm4.encrypt(data, SM4_KEY, {mode:'ecb', padding:'ZeroPadding'});
  return c;  // 密文以 base64 返回
}
$.post("https://api.acmecorp-test.com/api/v2/secure/submit", {data: enc(payload)});
```

扫描后你会看到（**实测输出**）：

```
 ② 引用的密码库
     (未识别到已知密码库指纹)
--------------------------------------------------------------------
 ③ 提取的密钥材料 (1 条)
   [高] 变量名上下文:SM4_KEY  【弱密钥】
        值    : 0123456789abcdeffedcba9876543210
        长度  : 16 字节 → SM4 / AES-128 / DES-EDE2（16 字节密钥）
--------------------------------------------------------------------
 ④ 加密逻辑总结
   主算法  : SM4
   模式    : ECB
   填充    : ZERO
   密文编码: BASE64
   >>> 规则: SM4-ECB + ZERO + 密钥来自「变量名上下文:SM4_KEY」 + 密文编码 BASE64
   [注意] 该组合（ECB + ZERO）不含完整性校验：密钥错误时不会报错，只会解出乱码。
          复现时请人工判断明文是否符合业务预期。
--------------------------------------------------------------------
 ⑤ 可直接填入加解密工作台的配置
   --alg sm4 --op dec --mode ecb --padding zero --key 0123456789abcdeffedcba9876543210 --iv (缺) --encoding base64
   [注意] 该组合不含完整性校验，解密不报错 ≠ 密钥正确，请人工核对明文。
--------------------------------------------------------------------
 ⑥ 建议的 MITM 白名单域名 (1 个)
   · api.acmecorp-test.com
```

关键点：**模式是 ECB、填充是 Zero，跟默认的 CBC+PKCS7 完全不同**——报告直接把
`--mode ecb --padding zero` 写进了第 ⑤ 段，点 **一键填入 MITM** 后 Tab 2 里
工作模式/填充也会同步为 ECB/Zero，不用你再猜。

> **验证一下**：把 ⑤ 段那条命令粘到命令行，加密 `hello world` 得到
> `RzFLNQQYCfWdX1o0NeuInQ==`；再用同一组参数解密，能原样还原 `hello world`。
> 说明扫描得出的模式/填充**是可复现的正确值**，不是猜的。

### 6.4 命令行等价用法

```bash
cd "%APP%\resources\python"
set PY=%APP%\resources\python-embed\python.exe

REM ① 自检（验证扫描引擎）
%PY% file_crypto_scanner.py --selftest

REM ② 扫描文件，人读报告
%PY% file_crypto_scanner.py scan --file D:\app.js

REM ③ 扫描文件，机器读 JSON（GUI 就是这么调的）
%PY% file_crypto_scanner.py scan --file D:\app.js --json

REM ④ 深度扫描（钻进 ZIP/APK 内层）+ 限制读取体积
%PY% file_crypto_scanner.py scan --file D:\app.apk --deep --max-bytes 5242880 --json
```

**实测输出（扫一个含 SM4 硬编码密钥的 JS）**：
```
 识别类型    : JavaScript 源码  [js, 置信度 high]
 熵值        : 5.5324 —— 中等（可能是压缩文本或结构化二进制）
--------------------------------------------------------------------
 ① 识别的密码算法 (5 项)
   [高] 国密:SM4        命中 gmssl.sm4.encrypt
   [高] 填充:PKCS7      函数名 sm4.encrypt 默认 PKCS7
   [中] 模式:CBC        选项 mode: 'cbc'
   [中] 编码:Base64     注释「密文以 base64 返回」
   [低] 哈希:MD5        字符串 md5
--------------------------------------------------------------------
 ② 引用的密码库
   · gmssl (Python 国密库)  (关键字: gmssl)
   · crypto-js (JS 密码库)  (关键字: crypto-js)
--------------------------------------------------------------------
 ③ 提取的密钥材料 (2 条)
   [高] 变量名上下文:SM4_KEY  【弱密钥】
        值    : 0123456789abcdeffedcba9876543210
        长度  : 16 字节 → SM4 / AES-128 / DES-EDE2（16 字节密钥）
        弱密钥: 国标示例密钥，非生产密钥
   [高] 变量名上下文:IV
        值    : fedcba98765432100123456789abcdef
--------------------------------------------------------------------
 ④ 加密逻辑总结
   主算法  : SM4
   模式    : CBC
   填充    : PKCS7
   密文编码: BASE64
   >>> 规则: SM4-CBC + PKCS7 + 密钥来自「变量名上下文:SM4_KEY」 + IV 来自「变量名上下文:IV」 + 密文编码 BASE64
--------------------------------------------------------------------
 ⑤ 可直接填入加解密工作台的配置
   --alg sm4 --op dec --mode cbc --padding pkcs7 --key 0123456789abcdeffedcba9876543210 --iv fedcba98765432100123456789abcdef --encoding base64
--------------------------------------------------------------------
 ⑥ 建议的 MITM 白名单域名 (1 个)
   · api.acmecorp-test.com
--------------------------------------------------------------------
 ⑦ 风险提示 (2 项)
   [高] 存在硬编码密钥材料
   [高] 存在弱密钥
--------------------------------------------------------------------
 ⑧ 建议下一步: 提取到对称加密密钥，可直接填入加解密工作台验证
```

**再扫一个没有加密的普通 JS**（`main.js`），工具会**明确说"未识别"而不是瞎猜**：
```
 ① 识别的密码算法 (0 项)
     (未识别到密码算法特征)
 ② 引用的密码库
     (未识别到已知密码库指纹)
 ③ 提取的密钥材料 (0 条)
     (未提取到硬编码密钥)
 ④ 加密逻辑总结
   主算法  : 未识别
   模式    : 未识别      ← 不会默认显示 CBC 骗你
   填充    : 未识别
   密文编码: 未识别
```

> **设计原则**：报告分段编号 ①~⑨ 永远连续（无命中的段落也会显示"未识别"），
> 且**绝不把「默认值」当成「识别结果」**。看到"未识别"就是真的没找到，可以放心。

### 6.5 关键参数

| 参数 | 说明 |
|---|---|
| `--file` | 待扫描文件路径（必填） |
| `--deep` | 深度扫描：钻 ZIP/APK 内层、加大采样窗口 |
| `--max-bytes N` | 最大读取字节数，默认 8MB，防止超大文件卡死 |
| `--json` | 输出结构化 JSON（含 `apply_crypto` / `apply_mitm` 自动填入配置） |

> **JSON 结构速查**（写脚本对接时用得上）：
> - 顶层：`file`（文件概况）、`algorithms`、`secrets`、`logic`、`risks`、`route`、
>   `domains`、`inner_files`、`crypto_libs`、`status`
> - 自动填入配置在 **`logic.apply_crypto`** 和 **`logic.apply_mitm`** 下（注意在 `logic` 里，不是顶层）
> - `route.tab` / `route.action` 告诉 GUI 该跳哪个页签

---

### 6.6 伪装常量与密钥派生链（v3.1 新增）

很多目标的密钥**既不是明文常量，也不是配置项**，而是这两种情况之一：

| 情况 | 长什么样 | 以前怎么办 |
|---|---|---|
| **伪装常量** | `data:image/png;base64,MTJjMTRhMjQ0Yjg0...`（解码出来根本不是 PNG）；裸 base64 串；长 HEX 串 | 人眼看到"base64"以为是图片，直接跳过 |
| **派生密钥** | `master = SHA256(AES-CBC(种子, 常量A, 常量B))`，真正的密钥是 `master[16:32]` | 只能识别出"用了 AES"，密钥永远差一步 |

现在扫描结果面板底部会多出「**伪装常量与密钥派生链**」一块：

- **伪装常量**：自动解码，并判断"声明的 MIME 与真实内容是否对得上"（对不上就是伪装）；
  解出来是文本时，还会**按常见拼法给出定长切片**——例如 67 个字符会切成
  `[0:32] 业务号/种子`、`[32:48] 疑似密钥(16 字节)`、`[48:64] 疑似 IV`、`[64:67] 固定后缀`，
  候选密钥旁直接有「复制」按钮。
- **派生链**：识别「对称加密 → 摘要 → 取字节区间」这条链，列出每一步，并生成
  **可直接执行的 `cli_crypto.py --derive` 命令**（复制即可用）。
- 若同一份文件里既有伪装常量、又有派生链，工具会**自动把切出来的常量填进命令占位符**，
  置信度也会同步提高——这一步通常就是"从差一步到真的解开"。

> **二进制/字节码也扫**：`.class` / `.dex` / `.jar` / `.apk` / 无后缀二进制会先抽可打印串再分析，
> Java class 常量池里的字符串就是这么拿出来的。压缩包会逐个条目分析。

> **误报控制**：真的 PNG/JPG data URI 不会被当成伪装；散文类文件（README/PRD）只用来找常量、
> 不做派生链识别（否则满篇的"SM4、AES、SHA256"会刷屏）；同一份常量被换行拆成两段时会**自动合并**后再解码。

---

## 七、Tab 5「文件夹整体分析」怎么用（多文件串联）

**一句话**：把**整个项目目录**丢进去，工具会递归分析其中所有文件，并找出它们之间
**串联起来的密码逻辑**——密钥定义在 A、加解密在 B、发请求在 C，单看一个文件永远看不到全貌。

**为什么需要这个页签**：Tab 4 一次只看一个文件，得到的是碎片。真实项目里
「密钥材料 / 密码器初始化 / 加解密 / 编码 / 签名 / 传输」常常分散在不同文件里，
只有放在一起，才能回答「这条数据到底怎么被保护的」「薄弱环节在哪」。

**v1.2 起它还能反过来用**：把整体分析出的**加密逻辑**反推成可执行的**解密逻辑**，
并且支持**直接粘贴密文出明文**（详见 §7.5）。

### 7.1 它能给出什么

| 输出 | 说明 |
|---|---|
| **跨文件关系图** | 四种边：`import 依赖` / `共享密钥`（同一密钥字面量出现在 ≥2 文件，**高风险**）/ `符号引用`（A 定义、B 引用）/ `同用密码库`。左→右 = 依赖方向，悬停节点高亮其关系 |
| **数据流链路** | 按 `取密钥 → 初始化密码器 → 加解密 → 编码 → 签名 → 传输` 六种角色聚合，标出每个阶段涉及哪些文件 |
| **串联链路追溯** | 某个密钥 / 符号从「定义方 → 使用方」的完整路径 |
| **整体总结** | 在线 LLM 给出体系 / 数据流 / 密钥管理 / 跨文件逻辑 / 算法 / 风险六段结论；离线则用规则兜底 |
| **解密逻辑（v1.2 新增）** | 把上述加密逻辑**反过来**写成可执行步骤（先解哪层编码 → 用哪把密钥/IV → 什么模式填充 → UTF-8 出明文），并把跨文件的「算法」与「密钥」**拼成完整配方** |
| **粘贴密文一键解密（v1.2 新增）** | 密文直接粘进去就出明文：自动试遍「配方 × 输入形态（Base64 / Hex / 二次 Base64）」，按明文特征打分选出正确的那次 |
| **文件清单表格** | 每个文件的相关度、识别到的算法、密钥数、角色标签，可一键跳「单文件深析」 |
| **整体证据包预览** | 本次实际交给 AI 的内容（分析范围 → 关系图 → 链路 → 解密配方 → 重点文件明细），可复制 |

### 7.2 操作步骤

1. 进入 **Tab 5 文件夹整体分析**。
2. 点「**浏览文件夹**」，选择项目根目录
   （如前端工程、小程序解包目录、逆向出来的 `assets` 目录）。
3. 按需调整选项：

   | 选项 | 默认 | 说明 |
   |---|---|---|
   | 深度扫描 | 关 | 打开后会钻入压缩包 / APK 内部条目 |
   | 单文件上限 (MB) | 4 | 超过则跳过，并在结果里记录原因 |
   | 最多文件数 | 200 | 超出按后缀优先级截断，避免卡死 |

4. 点「**开始整体分析**」。页面显示百分比进度 + 分阶段日志
   （遍历 → 逐文件摘要 → 建关系图 → 重建链路 → 在线 LLM 整体总结）。
5. 阅读结果：**整页可上下滚动**；关系图区域本身还能横向 / 纵向单独滚动
   （图按 1:1 像素渲染，不缩放，文字始终清晰）。
6. 往下找到「**解密逻辑**」区块，点配方上的「用此配方解密」；
   再把密文粘到「**粘贴密文，直接解密**」输入框，点「解密」即可看到明文（§7.5）。

> **自动排除的噪声目录**：`node_modules`、`.git`、`dist`、`build`、`venv`、
> `Pods` 等会在遍历时直接跳过，既不拖慢分析，也不会污染关系图。

### 7.2.1 派生配方（v3.1 新增）

除了原有的「算法 × 密钥」配方，文件夹分析现在还会输出**派生配方**：

```
派生密钥 · AES-CBC(种子)→SHA256→取[16:32]      置信度 0.95
密钥: 4A7F2C1E9B3D5A08 | IV: C6B1D90E37F2A485
命令: cli_crypto.py --derive aes-cbc-then-sha256-slice --seed <种子> \
      --derive-key 4A7F2C1E9B3D5A08 --derive-key-format utf8 \
      --derive-iv C6B1D90E37F2A485 --derive-iv-format utf8 --slice 16:32
```

它的 `key_hex` 是「**派生用的常量**」而不是最终密钥，所以卡片上标的是
「密钥需先派生」而不是「可直接解密」，按钮也换成了 **「去 Tab 8 派生密钥」**——
点一下会把派生方式与常量自动填进 Tab 8，你只要补上**种子**（报文里那个每次变化的随机值），
就能复算出真正的密钥并一键填回工作台。**这就是"只识别出算法"与"真的能解开"之间的那一步。**

跨文件绑定是这一步的关键：常量藏在 `Config.class`、派生链线索在 `Crypto.class`，
单看任一文件都拼不出配方，合起来才能给出完整链路。

### 7.3 结果怎么看（重点）

- **橙色实线** = 共享密钥边，**这是最危险的信号**：同一个密钥字面量出现在多个文件里，
  说明密钥已随代码扩散，一处泄露即全线失守。
- **灰色虚线** = 仅「同用某个密码库」，属弱关联，参考价值低。
- **数据流链路缺段** = 保护不完整。例如链路停在「编码」而没有「传输」，
  说明该数据可能被明文外发。
- **整体总结里的风险段**是跨文件才能得到的结论，例如：
  - 对已经是 Base64 的密文又做了一次 `btoa`（双重编码）；
  - 加密密钥与 HMAC 签名密钥是同一个（加密/认证密钥未分离）；
  - 部分接口走 GET 未加密（保护范围不完整）；
  - 命名暗示 AES-256，但密钥实际只有 16 字节（其实是 AES-128）。

### 7.4 命令行等价用法

```bash
cd "%APP%\resources\python"
set PY=%APP%\resources\python-embed\python.exe

REM ① 只做离线结构分析（不联网）
%PY% folder_crypto_analyzer.py --dir "D:\project\src" --no-llm

REM ② 在线整体分析（默认，走 ai_config.json 里的 LLM）
%PY% folder_crypto_analyzer.py --dir "D:\project\src" --json

REM ③ 先看「AI 到底看到了什么」——只打印证据包，不调 LLM
%PY% folder_crypto_analyzer.py --dir "D:\project\src" --show-evidence

REM ④ 深度扫描（钻压缩包）+ 放宽文件上限
%PY% folder_crypto_analyzer.py --dir "D:\project\src" --deep --max-files 400

REM ⑤ 自检（验证遍历 / 关系图 / 链路逻辑 / 解密配方 / 解密执行）
%PY% folder_crypto_analyzer.py --selftest

REM ⑥ 只看解密配方（加密逻辑 ↔ 解密逻辑 对照，不调 LLM 时最快）
%PY% folder_crypto_analyzer.py --dir "D:\project\src" --no-llm --show-plans

REM ⑦ 用上次分析出的配方直接解密（不用重新遍历目录，秒回）
%PY% folder_crypto_analyzer.py --decrypt-text "d3U3aG1KaFpx..."
%PY% folder_crypto_analyzer.py --decrypt-file "D:\cipher.txt" --plan "AES-128-CBC / PKCS7 / Base64"
```

### 7.5 解密逻辑与「粘贴密文直接解密」（v1.2 新增）

**这一步是整个页签最有用的产出**：分析完不只是告诉你「用了什么加密」，
而是给你一份**能直接执行的解密配方**，以及一个「粘密文就出明文」的输入框。

#### ① 跨文件拼接：配方是怎么凑齐的

真实项目里，**算法和密钥通常不在同一个文件**：

```js
// config/crypto.config.js —— 只有密钥，没有任何加密调用
export const AES_KEY = '1a2b3c4d5e6f708192a3b4c5d6e7f809';
export const AES_IV  = '0f1e2d3c4b5a69788796a5b4c3d2e1f0';

// src/utils/cipher.js —— 只有算法，没有密钥
var key = CryptoJS.enc.Hex.parse(AES_KEY);
var iv  = CryptoJS.enc.Hex.parse(AES_IV);
CryptoJS.AES.encrypt(plain, key, { iv: iv, mode: CryptoJS.mode.CBC, padding: CryptoJS.pad.Pkcs7 });
```

单看任一文件都做不出可执行配方。工具会顺着 `import` / 符号引用边把两边拼起来，
并且**按密钥真实长度重算算法位宽**——本例密钥是 16 字节，所以是 **AES-128**，
而不是按名字默认猜的 AES-256。

> 另一个必须靠源码语义判断的点：`AES_KEY` 这串字符到底按 **Hex** 还是 **UTF-8** 解析？
> 工具会看源码里是 `enc.Hex.parse` 还是 `enc.Utf8.parse`——
> 前者 16 字节密钥，后者 32 字节文本，长度不同 → 算法位宽也不同。

#### ② 配方卡片上会显示什么

| 字段 | 含义 |
|---|---|
| 加密逻辑 | 逐条列出加密侧做了什么（取密钥 → 取 IV → 加密 → 编码 → 外发） |
| **解密逻辑（逆序）** | 把上面倒过来，就是你要照着做的操作：先解编码、再解密、最后 UTF-8 |
| 密钥(HEX) / IV(HEX) | 从证据里**逐字提取**的原文，可直接复制；并标注来源（如「变量名上下文:AES_KEY」） |
| 涉及文件 | 这条配方是从哪几个文件拼出来的（体现「跨文件」价值） |
| 置信度 + 依据 | 依据说明为什么可信（几字节密钥、几个文件互证、角色链是否完整） |
| 无完整性校验 | 该组合（如 ECB+Zero）密钥错了**不会报错**，只出乱码 —— 必须靠内容判断 |
| 缺 IV / 缺密钥 | 配方不完整，按钮会禁用（不会给你一个点了必然失败的按钮） |

#### ③ 粘贴密文直接解密

1. 点配方上的「**用此配方解密**」（或让方案选择框停在「自动尝试全部配方」）。
2. 把抓包 / 接口返回里的密文**整段**粘到输入框。
3. 点「**解密**」。

**你不需要告诉它密文是什么编码**。工具会：

- 对每条可执行配方 × 每种输入形态（`Base64`、`Hex`、`Base64→Base64`、`Raw`）逐个尝试；
- 每次解密结果都做**明文特征打分**：UTF-8 合法性、可打印比例、是否 JSON/表单结构、
  是否命中 `orderId`/`token`/`phone` 这类业务字段名；
- 取分数最高的一次作为答案，并把其它也解出明文的尝试列出来供交叉验证。

> **为什么必须打分而不是「没报错就算成功」**：CBC / ECB 用错密钥**不会抛异常**，
> 只会解出一堆乱码。所以判定标准必须是「解出来的东西像不像业务数据」。

失败时不会只说一句「失败」，而是给出 **尝试记录**（每次用了哪条配方、哪种输入形态、
失败原因），并提示四种常见原因：密文还有未识别的编码层、密钥不在目录里（服务端下发）、
接口本身未加密、密文被截断。

#### ④ 拿到明文之后

- **复制明文**：直接取用。
- **格式化 JSON**：明文是 JSON 时一键缩进，看字段方便。
- **填入工作台**：把这条配方的算法/模式/填充/密钥/IV/编码灌进 Tab 1，
  方便你改参数反复对比试解。
- **复制 cli_crypto 命令**：给出一条可直接在命令行复现的命令（含真实 key/iv）。

### 7.6 关键参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `--dir` | 必填 | 要整体分析的目录 |
| `--deep` | 关 | 钻入压缩包 / APK 内部条目 |
| `--max-files` | 200 | 最多分析文件数，超出记入 `dropped_count` |
| `--max-file-mb` | 4 | 单文件体积上限 |
| `--evidence-chars` | 24000 | 交给 AI 的证据包字符预算 |
| `--no-llm` | 关 | 只用离线规则，完全不联网 |
| `--show-evidence` | 关 | 只打印证据包、不调 LLM（排查「AI 看到了什么」） |
| `--show-plans` | 关 | 分析完只输出解密配方（加密逻辑 ↔ 解密逻辑），快速取用 |
| `--plans-out` | 临时目录 | 解密配方落盘位置（`JIEJIEEAD_PLANS_PATH` 可覆盖） |
| `--decrypt-text` | — | 用配方解密密文（不带值则从标准输入读） |
| `--decrypt-file` | — | 从文件读取密文（二进制 / 大密文用这个） |
| `--plans` | 上次落盘 | 指定配方文件路径（默认读 `--plans-out` 那份） |
| `--plan` | `auto` | 只用指定配方（按 label 匹配），默认自动尝试全部可执行配方 |
| `--selftest` | — | 跑自检用例（84 项） |

> **提示**：本页签与 Tab 6 AI 智能分析**共用同一份 LLM 配置**（`ai_config.json`），
> 在任一处改读超时 / 模型 / Key，另一处立即生效。

---

## 八、Tab 6「AI 智能分析」怎么用

**一句话**：把一段代码 / 抓包 / 配置文件粘进去，工具自动判断它用了哪套对称密码方案
（算法 / 工作模式 / 填充 / 密钥 / IV / 编码），并把结果一键填到 Tab 1 工作台或 Tab 2 MITM。

这是「文件扫描」的自然延伸——不需要先把片段存成文件，直接贴文本即可；适合拿到一段
JS / 抓包文本 / 配置，想快速知道"该选 SM4-CBC 还是 SM4-ECB+Zero、密钥在哪"。

### 8.1 两种分析来源（在线为主 + 离线兜底）

| 来源 | 何时用 | 依赖 |
|---|---|---|
| **在线 LLM** | 配置了 Base URL + API Key 且启用时 | 任意 OpenAI 兼容接口（OpenAI / OpenRouter / Ollama / 本地模型） |
| **离线启发式** | 未配置在线，或在线调用失败 | 复用 `file_crypto_scanner.py` 的静态规则，**零网络、零额外依赖** |

> **融合策略**：配了在线就先走 LLM；LLM 调用失败（网络/鉴权/解析异常）会**自动降级**
> 到离线规则，并在结果里标记 `source = offline-fallback`，**绝不静默失败**。离线结论
> 与 Tab 4 文件扫描器完全一致（同源）。

### 8.2 配置区（可选，可折叠）

点结果栏右侧「配置」开关展开：

| 字段 | 说明 |
|---|---|
| **启用在线** | 关闭则只走离线启发式 |
| **Base URL** | OpenAI 兼容地址，如 `https://api.openai.com/v1`、OpenRouter、或 `http://localhost:11434/v1`（Ollama） |
| **API Key** | 对应平台的密钥；留空表示不修改已保存的密钥 |
| **模型** | `gpt-4o-mini` / `claude-3.5` / `qwen-...` 等 |

点 **保存配置** 写入本地 `python/ai_config.json`（可用环境变量 `JIEJIEEAD_AI_CONFIG`
覆盖路径），**不随 EXE 打包**。点 **读取已保存** 把已存配置载入（密钥被遮掩，不回显）。

> **发布版不含任何预设密钥**：`ai_config.json` 里 `api_key` 为空、`enabled=false`，
> 只保留字段模板；在线能力需要你自己填。这样做是为了让包可以安全分发——
> 否则拿到包的人等于同时拿到了你的 Key。
>
> 自己的密钥建议**放在包外**，用环境变量指向，升级/覆盖安装都不会丢，也不会被误打包：
>
> ```bash
> set JIEJIEEAD_AI_CONFIG=D:\mykeys\jiejieead_ai.json
> ```
>
> 对应地，`python/crypto_knowledge.json` 在发布版里也是**空库**（0 条），
> 因为它会保存「从真实目标学到的算法/密钥/IV」，同样不适合随包分发。

### 8.3 操作步骤

1. 切到 **✨ AI 智能分析** 页签
2. 把待分析的代码 / 抓包 / 配置片段粘进文本框
3. 点 **智能分析**
4. 看结果卡片：
   - 顶部徽标显示**来源**（在线 LLM / 离线规则 / 离线兜底）+ **置信度**
   - 算法 / 工作模式 / 填充 / 编码 / 密钥 KEY / 向量 IV 逐项列出
   - **识别证据** 给出支持该判断的依据；**风险与提示** 标出硬编码密钥、弱密钥、ECB 等隐患
5. 拿到密钥后：
   - **一键填入工作台** → 自动切到 Tab 1 并把算法/模式/填充/密钥/IV/编码填好
   - **一键填入 MITM** → 自动切到 Tab 2 并把配置填好

### 8.4 命令行等价用法

```bash
cd "%APP%\resources\python"
set PY=%APP%\resources\python-embed\python.exe

REM ① 保存在线 LLM 配置
%PY% ai_crypto_analyzer.py --config-save base_url=https://api.openai.com/v1 api_key=sk-xxx model=gpt-4o-mini enabled=true

REM ② 查看当前配置（密钥遮掩）
%PY% ai_crypto_analyzer.py --config-show

REM ③ 分析一段文本（离线也要能跑，--json 给 GUI 调用）
%PY% ai_crypto_analyzer.py --analyze --text "var SM4_KEY='0123456789abcdeffedcba9876543210'; sm4.encrypt(d,SM4_KEY,{mode:'ecb',padding:'ZeroPadding'});" --json

REM ④ 不写配置也能离线分析（从文件读）
%PY% ai_crypto_analyzer.py --analyze --file D:\app.js --json

REM ⑤ 自检（离线规则 + 在线降级全部验证）
%PY% ai_crypto_analyzer.py --selftest
```

**③ 实测输出（离线规则，未配在线）**：
```json
{"status": "ok", "source": "offline", "alg": "sm4", "mode": "ecb", "padding": "zero",
 "key": "0123456789abcdeffedcba9876543210", "iv": null, "encoding": "base64",
 "confidence": 0.62, "hints": ["SM4（高，命中 sm4.encrypt）", "模式:ECB（中，选项 mode:'ecb'）"],
 "warnings": ["该组合（ECB + ZERO）不含完整性校验：密钥错误时不会报错，只会解出乱码。"]}
```
> 结果里的 `source` 字段：在线成功为 `online`，离线为 `offline`，在线失败降级为
> `offline-fallback`，**命中本地知识库为 `learned`**。GUI 顶部徽标就是按这个字段显示来源与配色的。

### 8.5 本地知识库：自动学习与命中复用（v1.1 新增）

**解决的痛点**：同一个项目/同一套前端库的加密逻辑，会在多个文件、多轮测试里反复出现。
每遇到一次都重新分析纯属浪费。知识库把「**已确认正确的结论**」沉淀到本地，下次遇到
**同一段逻辑**直接命中，秒出算法/模式/填充/密钥/IV。

**两级签名**：

| 签名 | 依据 | 作用 |
|---|---|---|
| **内容签名** | 归一化后的文本（去 JS 注释、抹掉全部空白、转小写）取 SHA-256 前 32 位 | **精确命中**，可复用完整结论（含密钥/IV）。排版/大小写/换行变了也能命中 |
| **方案签名** | `alg\|mode\|padding\|encoding`（如 `sm4\|ecb\|zero\|base64`） | **方案级旁证**：内容不完全一致但方案相同，会给结果追加「该方案历史出现 N 次」提示，**不**套用旧密钥 |

**什么时候会自动学习**：

| 触发 | 来源标记 |
|---|---|
| GUI 点「**学习此规则**」按钮 | `manual` |
| GUI 点「一键填入工作台 / MITM」（视为接受该结论） | `manual` |
| 在线 LLM 结果且置信度 **≥ 0.8** | `online` |

> 离线启发式结果**不自动入库**（置信度不足），需人工确认后再学习——避免把"看起来像"的
> 错误结论沉淀成"经验"。

**怎么用**：

1. 分析出正确结论后，点结果卡片里的「**学习此规则**」（或直接点「一键填入」）
2. 提示「已记录到本地知识库（共 N 条规则）」
3. 之后再遇到**同样逻辑**，顶部徽标显示 **`知识库命中`**，置信度 1.00，直接给出历史密钥
4. 结果卡片下方会显示：`本地知识库：N 条规则 · M 种方案 · 累计命中 K 次`，可点「清空知识库」

**命令行等价用法**：

```bash
cd "%APP%\resources\python"
set PY=%APP%\resources\python-embed\python.exe

REM 学习：分析并入库（也可用 --as-json 指定要入库的结果做人工校正）
%PY% ai_crypto_analyzer.py --learn --text "var K='a1b2...'; AES.encrypt(d,K,{mode:CBC,padding:Pkcs7});"
%PY% ai_crypto_analyzer.py --learn --file D:\app.js --as-json "{\"alg\":\"sm4\",\"mode\":\"ecb\",\"padding\":\"zero\"}"

REM 查看 / 统计 / 清空
%PY% ai_crypto_analyzer.py --kb-list
%PY% ai_crypto_analyzer.py --kb-stats --json
%PY% ai_crypto_analyzer.py --kb-clear
%PY% ai_crypto_analyzer.py --kb-path

REM 强制绕过知识库（调试时对比：看纯启发式的结果）
%PY% ai_crypto_analyzer.py --analyze --text "..." --no-learn --json
```

**实测闭环**：
```
① 首次分析 → source=offline, alg=aes128/cbc/pkcs7, key=a1b2...（confidence 0.6）
② --learn  → [OK] 已学习并入库 · 内容签名 dfe07f4c... · 方案 aes128|cbc|pkcs7|base64
③ 再次分析 → source=learned, confidence=1.0, key=a1b2...（复用历史结论，直接命中）
④ --kb-stats → 规则条目 1 / 方案种类 1 / 累计命中 1 次
```

**知识库文件位置**：
- 优先脚本同目录 `resources/python/crypto_knowledge.json`（免安装版可写）
- 若脚本目录不可写（如装在 `Program Files`），自动回退到
  `%LOCALAPPDATA%\jiejieEAD\crypto_knowledge.json`
- 可用环境变量 `JIEJIEEAD_KB_PATH` 强制指定
- 纯本地 JSON，**不随 EXE 打包、不上传任何数据**

---

## 九、MCP 接入（把本工具交给 AI 客户端调用）

**是什么**：本工具内置一个 **stdio MCP 服务器** `crypto_mcp_server.py`，把密码能力
（加解密 / 文件扫描 / 能力矩阵）暴露为 MCP 工具，供 **WorkBuddy / Claude Desktop / 任意 MCP 客户端**
以「模型上下文协议」方式调用。接好之后，可以在客户端里直接说
「用 jiejieEAD 解密这段 SM4 密文」，由本服务器真实执行。

### 9.0 图形化接入（推荐：GUI Tab 7「MCP 接入」）

打开 GUI 最后一个页签 **MCP 接入**，不用手抄路径、不用手改 JSON：

| 步骤 | 操作 | 作用 |
|---|---|---|
| 1 | 打开页签（自动体检） | 显示 mcp SDK 是否就绪、h11/httpcore 版本、`pip check` 是否全绿、服务器脚本是否齐全 |
| 2 | **一键安装 / 修复依赖** | 安装 `mcp`，并自动把 `h11` / `httpcore` 压回与 mitmproxy 共存的兼容版本 |
| 3 | **stdio 握手自检** | 真实拉起服务器：initialize → tools/list → 真加密 → 错误密钥必须被拒，全过才算“接入可用” |
| 4 | **写入 WorkBuddy / 写入 Claude Desktop** | 按本机真实绝对路径生成并**合并**写入客户端配置（保留其它服务器 + 自动备份 `.bak-jiejieead`） |

> 写入后需要**重启客户端**，或在连接器管理页右上角「自定义连接器」里对新增的
> `jiejieead` 服务器点 **「信任」**，才会真正生效。
>
> 页签里的「复制配置」也支持手动粘贴：生成的 JSON 已带好
> `PYTHONIOENCODING=utf-8` / `PYTHONUTF8=1`（工具返回的中文报错信息在部分
> Windows 客户端下若按 GBK 解码会乱码）。

> **免安装版开箱可用**：`mcp`（约 20MB）已随包内置在 `resources\python-embed`，
> 打开页签即是「就绪」。只有需要重装/修复时才点第二个按钮。

### 9.1 命令行方式（与图形化等效，供脚本 / CI 使用）

```bash
cd "%APP%\resources\python"
set PY=%APP%\resources\python-embed\python.exe

REM 体检：SDK / 依赖兼容性 / 脚本 / 客户端登记情况（JSON 可用 --json）
%PY% mcp_setup.py --status

REM 安装/修复依赖（等效于 GUI 的「一键安装/修复依赖」按钮）
%PY% mcp_setup.py --install

REM 生成可粘贴的客户端配置（自动填本机真实路径）
%PY% mcp_setup.py --config

REM 直接写入客户端配置：workbuddy / claude / <配置文件绝对路径>
%PY% mcp_setup.py --write workbuddy

REM 真实 stdio 握手自检（等效于 GUI 的「stdio 握手自检」按钮）
%PY% mcp_setup.py --smoke

REM 接入向导自身自检（体检/配置生成/写入合并/握手冒烟）
%PY% mcp_setup.py --selftest

REM 只跑服务器本身（被客户端拉起，不要手动跑）
%PY% crypto_mcp_server.py --selftest
%PY% crypto_mcp_server.py
```

> **⚠️ 装完 mcp 要补一步（否则模块二 MITM 会坏）**
> `mcp` 会连带把 `h11` 升到 0.16、`httpcore` 升到 1.0.9，而 `mitmproxy 11.0.2` 要求
> `h11<=0.14.0`，pip 会报 `dependency conflicts`。装完 mcp 后再补一条压回兼容组合：
>
> ```bash
> %PY% -m pip install "h11==0.14.0" "httpcore==1.0.7"
> ```
>
> 实测这套组合下 `pip check` 输出 **No broken requirements found**，
> 且 `mcp`（FastMCP + stdio 客户端）与 `mitmproxy` **两边都能正常导入运行**，
> stdio 握手与 4 个工具调用全部通过。若只跑 MCP、不用 MITM，可忽略此步。

### 9.2 暴露的 4 个工具

| MCP 工具 | 作用 | 关键入参 |
|---|---|---|
| `crypto_encrypt` | 对称加密 | `alg` / `key` / `text` / `mode` / `padding` / `iv` / `encoding` |
| `crypto_decrypt` | 对称解密 | 同上 |
| `scan_crypto_file` | 分析文件密码特征（算法/密钥/可回填配置） | `path` |
| `list_capabilities` | 返回算法/模式/填充/编码能力矩阵 | — |

> 四个工具全部复用 `crypto_core.py` / `file_crypto_scanner.py`，**与 GUI 同源**，结果一致。

### 9.3 接入 WorkBuddy / Claude Desktop

在客户端的 `mcpServers` 配置里加一项（command 指向你的 Python，args 指向本脚本）：

```json
{
  "mcpServers": {
    "jiejieead": {
      "command": "C:\\路径\\到\\python.exe",
      "args": ["C:\\路径\\到\\jiejieEAD2.0.0\\resources\\python\\crypto_mcp_server.py"]
    }
  }
}
```

接入后，客户端就能直接说"用 jiejieEAD 解密这段 SM4 密文"之类的话，由本服务器实际执行。

> **建议加上 env**：工具返回的 JSON 里含中文（例如「PKCS#7 去填充失败：填充字节非法…」），
> 在部分 Windows 客户端下 stdio 编码会走 GBK 导致乱码。显式声明 UTF-8 最稳：
>
> ```json
> {
>   "mcpServers": {
>     "jiejieead": {
>       "command": "C:\\Users\\<你>\\Desktop\\jiejieEAD2.0.0\\resources\\python-embed\\python.exe",
>       "args": ["C:\\Users\\<你>\\Desktop\\jiejieEAD2.0.0\\resources\\python\\crypto_mcp_server.py"],
>       "env": { "PYTHONIOENCODING": "utf-8", "PYTHONUTF8": "1" }
>     }
>   }
> }
> ```
>
> 注意：**免安装版不要单独拷 exe**，`resources\python-embed\python.exe` 与
> `resources\python\crypto_mcp_server.py` 都要在，路径按实际所在位置写。

### 9.4 实测输出

**`--selftest` 实测（节选）**：
```
   [ OK ] SM4-CBC 加密 == 2wZpvYQeVFDsFlLs1CttYA==
   [ OK ] SM4-CBC 解密还原 == hello
   [ OK ] SM4-ECB+Zero 加密 == RzFLNQQYCfWdX1o0NeuInQ==
   [ OK ] 能力矩阵字段齐全
   [ OK ] scan 识别 SM4
 [PASS] MCP 服务器底层能力自检通过。
```

**真实 stdio 握手（initialize → tools/list → 调用）实测**：
```
   [ OK ] initialize 成功，server=jiejieEAD
   [ OK ] tools/list = ['crypto_decrypt', 'crypto_encrypt', 'list_capabilities', 'scan_crypto_file']
   [ OK ] crypto_encrypt 端到端 == 2wZpvYQeVFDsFlLs1CttYA==
   [ OK ] list_capabilities 字段齐全
 [PASS] MCP stdio 握手 + 工具调用端到端通过。
```

**接入向导 `mcp_setup.py --selftest` 实测（本版新增）**：
```
   [ OK ] status 字段齐全
   [ OK ] 本环境已装 mcp SDK（v1.30.0）
   [ OK ] 依赖已压到与 mitmproxy 共存的组合（h11 0.14.0 / httpcore 1.0.7）
   [ OK ] pip check 无破损依赖
   [ OK ] 生成的 mcpServers JSON 可解析且路径真实存在
   [ OK ] command 指向 python.exe、args 指向服务器脚本
   [ OK ] env 已带 UTF-8 声明（防中文乱码）
   [ OK ] --write workbuddy 解析到 ~/.workbuddy/mcp.json
   [ OK ] 绝对路径原样透传
   [ OK ] 写入时保留其它服务器、自己入库、并生成备份
   [ OK ] smoke 返回结构化结果（永不抛异常）
        stdio 握手通过：server=jiejieEAD，5 步，耗时 1.3s
 [PASS] MCP 接入向导自检通过（体检 / 配置生成 / 写入合并 / 握手冒烟）。
```

**按真实客户端配置拉起（读 `~/.workbuddy/mcp.json` → 用里面的 command/args/env 起子进程）实测**：
```
    [ OK ] initialize → server=jiejieEAD v1.30.0
    [ OK ] tools/list (4) = ['crypto_decrypt', 'crypto_encrypt', 'list_capabilities', 'scan_crypto_file']
    [ OK ] crypto_encrypt → status=ok
    [ OK ] 错误密钥被拒 → status=error msg=PKCS#7 去填充失败：填充字节非法……
    [ OK ] list_capabilities → 8 个字段齐全
    ==> PASS
```

---

## 十、Tab 8「摘要 / HMAC / 密钥派生」怎么用

这一页解决的是**「密钥不是硬编码的，是算出来的」**这类最常见场景。
很多银行 / 政务 / App 的 H5 前端，密钥既不在配置里也不在代码里，而是运行时算出来的：

```
种子（报文里前置的随机数 / 时间戳 / 设备号）
   → 用固定常量做一次对称加密
   → 再摘要一次
   → 取中间若干字节
   → 才是真正的正文密钥
```

只盯着"外层用了 SM4"是解不开的。本页把这条链拆成三个可独立使用的工具。

### 10.1 密钥格式（先看这个，90% 的"密钥长度不合法"都在这）

同一串密钥文本，按不同格式解析出来的**字节数完全不同**：

| 密钥文本 | 按 HEX | 按 UTF-8 | 按 Base64 |
|---|---|---|---|
| `4A7F2C1E9B3D5A08` | 8 字节（AES-128 会直接报错） | **16 字节（正确）** | 12 字节 |
| `0123456789abcdeffedcba9876543210` | **16 字节（正确）** | 32 字节 | 24 字节 |

所以工作台与 MITM 的密钥输入框下面都加了**「密钥格式 / IV 格式」下拉**：

| 格式 | 什么时候用 |
|---|---|
| `HEX` | 密钥本来就是十六进制字符串（默认） |
| `UTF-8 字面量` | 代码里是 `KEY = "4A7F2C1E9B3D5A08"` 这样当**文本**用的（真实业务极常见） |
| `Base64` | 密钥是 Base64 字符串，解码后才是字节 |
| `自动判定` | 拿不准时用，按 HEX → Base64 → UTF-8 顺序试 |

工作台还会在读入密钥时**主动提示**："这串密钥按 HEX 是 8 字节、按 UTF-8 是 16 字节 —— 当前按 HEX 算，长度不合法，换个格式可能就对了"。
命令行等价用法：`--key-format utf8 --iv-format utf8`，或先跑 `--keyinfo --key <你的密钥>` 看三种解读。

```bash
%PY% cli_crypto.py --keyinfo --key 4A7F2C1E9B3D5A08
```

### 10.2 摘要 / 哈希

支持 `MD5 / SHA-1 / SHA-256 / SHA-512 / SM3`，输出可选 HEX 或 Base64。

```bash
%PY% cli_crypto.py --digest sm3 --text "abc"
%PY% cli_crypto.py --digest sha256 --text "abc" --out-encoding base64
%PY% cli_crypto.py --digest md5 --file payload.bin --json
```

| 算法 | 输出长度 | 备注 |
|---|---|---|
| MD5 | 16 字节 | 已不建议用于安全场景，界面会标注 ⚠；复现老系统逻辑仍需要 |
| SHA-1 | 20 字节 | 同上 |
| SHA-256 | 32 字节 | 最常用 |
| SHA-512 | 64 字节 | |
| **SM3** | 32 字节 | 国密摘要，与 GB/T 32905-2016 标准向量逐字节一致 |

### 10.3 HMAC（报文签名 / 完整性校验）

支持 `HMAC-MD5 / HMAC-SHA1 / HMAC-SHA256 / HMAC-SHA512 / **HMAC-SM3**`，输出 HEX 或 Base64。

```bash
%PY% cli_crypto.py --hmac hmac-sm3 --key <密钥> --key-format utf8 \
                   --text "待签名的原文" --out-encoding base64
```

> **HMAC-SM3 是国密体系里最常见的报文签名算法**（银行、政务 H5 大量使用），
> 之前本工具只能算 SM3 摘要、**没有 HMAC**，遇到"外层 mac = HMAC-SM3(master, head||cipher)"
> 这种结构就只能手工写脚本。现已内置，并与 BouncyCastle `HMac(SM3Digest)` 逐字节对齐。

HMAC 与"摘要 + 拼接"的区别、以及为什么不能自己拼：HMAC 用密钥对内外两层做异或填充（ipad/opad），
和 `SM3(key + msg)` 完全不是一回事——**签名对不上时，先确认是 HMAC 还是拼接摘要**。

### 10.4 密钥派生（本页的重点）

> 快捷入口：如果上一节「文件夹整体分析」已经识别出派生配方，直接点配方卡片上的
> **「去 Tab 8 派生密钥」**，派生方式与常量会自动填好，你只需要补种子。



选一个"派生方式"，把种子和常量填进去，就能把真实密钥算出来，并且**一键填进工作台 / MITM**。

内置预设：

| 预设 | 含义 | 需要额外填 |
|---|---|---|
| `SHA256(种子)` | 直接对种子摘要 | — |
| `MD5(种子)` / `SM3(种子)` | 同上，换算法 | — |
| `HMAC-SHA256(派生密钥, 种子)` | 用固定常量做 HMAC | 派生密钥 |
| `HMAC-SM3(派生密钥, 种子)` | 同上，国密 | 派生密钥 |
| `AES-128-CBC(种子) → SHA256` | 种子先 AES 加密再摘要 | 派生密钥 + IV |
| `AES-128-CBC(种子) → SHA256 → 取字节区间` | **白盒前端最常见** | 派生密钥 + IV + 取字节区间 |
| `SM4-CBC(种子) → SM3` | 国密版同款 | 派生密钥 + IV |

**取字节区间**按**字节**计数（不是字符），左闭右开：`16:32` 表示取第 17~32 字节。

界面会**逐步留痕**，每一步的算法、输入输出长度、结果前缀都列出来 —— 这一步是为了排查"到底从哪一步开始和对方不一样"：

```
派生方式：AES-128-CBC(种子) → SHA256 → 取字节区间
种子    ：0123456789abcdef0123456789abcdef（utf8）
  第 1 步 encrypt  AES128-CBC/PKCS7（密钥 16 字节、IV 16 字节） → 48 字节  34798a1325e425f3ecbec2fe989a7479…
  第 2 步 digest   SHA256                                      → 32 字节  cc898eb43fa8c1e55fbae2819daa059e…
  第 3 步 slice    [16:32]                                     → 16 字节  dd0d1b8e73aada9f96896feb61f1dbb9…
派生结果：dd0d1b8e73aada9f96896feb61f1dbb9（16 字节）
```

命令行等价用法：

```bash
%PY% cli_crypto.py --derive aes-cbc-then-sha256-slice \
      --seed 0123456789abcdef0123456789abcdef \
      --derive-key 4A7F2C1E9B3D5A08 --derive-key-format utf8 \
      --derive-iv  C6B1D90E37F2A485 --derive-iv-format utf8 \
      --slice 16:32
```

### 10.5 实战串联：从"只识别出 SM4"到"真的解开"

以「种子前置 + 常量派生」这一类报文为例，它的结构是：

```
报文   = 种子(32 字符) + Base64 密文
cipher = SM4-ECB-PKCS7( master[16:32], utf8(明文 JSON) )
master = SHA256( AES-128-CBC(utf8(种子), utf8(常量A), utf8(常量B)) )
```

用本工具复现的完整步骤：

1. **Tab 8 → 密钥派生**：派生方式选 `AES-128-CBC(种子) → SHA256 → 取字节区间`；
   种子填报文里前置的那 32 个字符，派生密钥填常量A（格式选 UTF-8 字面量），
   IV 填常量B，区间 `16:32`；
2. 点「执行派生」→ 得到 16 字节正文密钥，点**「填入工作台」**；
3. **Tab 1 → 加解密工作台**：算法 `SM4`、模式 `ECB`、填充 `PKCS7`、编码 `Base64`，
   把密文粘进去点解密 → 出明文；
4. **Tab 8 → HMAC**（报文外层带 mac 时）：32 字节的 mac 既可能是 `HMAC-SM3`
   也可能是 `HMAC-SHA256`，两个都试；密钥填派生出的 master（HEX），
   消息填 mac 实际覆盖的字节 → 结果与报文里的 mac 一致，说明整条链路复现正确。

第 1 步的"取字节区间"和第 4 步的 HMAC 是以前工具做不到的两件事——这正是补齐之后的差别。

---

## 十一、Tab 9「非对称 / 压缩 / 编码链 / 信封」怎么用

前九个页签解决"对称 + 摘要 + 派生"，这一页解决**非对称与压缩**——真实业务里它们几乎总是一起出现：

```
随机对称密钥 → 对称加密数据
             → 用服务端公钥（SM2 或 RSA）加密那把对称密钥   ← 混合加密信封（安全键盘）
明文 → gzip 压缩 → Base64 → 加密                             ← 压缩是编码链里的一环
```

### 11.1 SM2（国密非对称）

| 能力 | 说明 |
|---|---|
| 加解密 | 密文格式支持 **C1C3C2**（国标推荐）与 **C1C2C3**（老实现），长度相同、顺序不同 |
| 签名验签 | 默认走国标 **SM3 + Z 值**预处理；只给私钥时会自动推导公钥（否则验签必然失败） |
| DER 签名 | 勾选 `asn1` 处理 DER 编码的签名/密钥（部分网关要求） |
| 输入已是摘要 | 对方若"先 SM3 再签裸摘要"，勾选此项 |
| 密钥格式 | 公钥：128 位 HEX（可带 `04`/`0x` 前缀）/ Base64 / PEM；私钥：64 位 HEX / Base64 / PEM |

```bash
%PY% cli_crypto.py --keygen sm2                              # 生成测试密钥对
%PY% cli_crypto.py --sm2 enc --public-key <128位HEX> --text "hello" --json
%PY% cli_crypto.py --sm2 dec --private-key <64位HEX> --text <密文> --encoding hex
%PY% cli_crypto.py --sm2 sign --private-key <HEX> --text "原文" --json
%PY% cli_crypto.py --sm2 verify --public-key <HEX> --text "原文" --sig <128位HEX>
```

### 11.2 RSA

| 能力 | 说明 |
|---|---|
| 加密填充 | `pkcs1v15`（默认）/ `oaep`（带摘要校验，更安全） |
| 签名方案 | `pkcs1v15` / `pss`（随机化填充） |
| 摘要算法 | MD5 / SHA-1 / SHA-256 / SHA-384 / SHA-512 |
| **长文本** | **自动分段**（单块容量 = 模长 − 11，对应 JS 里的 `encryptLong2`），大文件请走 `--file` |

> ⚠ **PKCS#1 v1.5 不含完整性校验**：密钥不对时不一定报错，可能只返回空/乱码。
> 要确认密钥是否正确，请核对明文内容或改用 OAEP。工具在解密时会主动给出这条提示。

### 11.3 压缩与多级编码链

```bash
%PY% cli_crypto.py --zip compress --zip-algo gzip --text "hello" --json
%PY% cli_crypto.py --zip decompress --zip-algo gzip --text <上面的输出> --encoding base64
%PY% cli_crypto.py --chain zlib,base64 --text "<明文>" --json           # 编码（按顺序）
%PY% cli_crypto.py --chain zlib,base64 --op dec --text "<编码串>"        # 解码（自动反序）
```

- 压缩算法：`zlib`（头 78 9C）/ `gzip`（头 1F 8B）/ `deflate`（zlib 封装）/ `deflate-raw`（裸流，pako 默认）；
- **解压时算法标错会自动依次试其它三种**（真实数据经常标错），并列出每次尝试的失败原因；
- 编码链步骤：`zlib / gzip / deflate / deflate-raw / base64 / base64url / hex / url` 任意组合；
- 解码链的输入**原样**交给各步处理（不要自己先解一次，否则会解两遍——实测踩过这个坑）。

### 11.4 混合加密信封（安全键盘那一套）

```bash
%PY% cli_crypto.py --envelope enc --recipient sm2 --public-key <SM2公钥> \
      --alg sm4 --mode cbc --text "<敏感数据>" --json
# 输出：包裹密钥(wrapped_key_hex) / 对称密文(cipher_hex) / IV(iv_hex)

%PY% cli_crypto.py --envelope dec --recipient sm2 --private-key <SM2私钥> \
      --alg sm4 --mode cbc --wrapped-key <包裹密钥HEX> --cipher-text <密文HEX> --iv <IV的HEX>
```

- 接收方算法可选 `sm2` / `rsa`；对称算法支持 SM4/AES/DES/3DES 的全部模式与填充；
- 加密时会**顺手把三个字段填到解密侧**（界面），CLI 下照着输出粘贴即可；
- 解密需要**接收方私钥**——这正是"拿到客户端/服务端密钥后能还原敏感字段"的完整链路。

### 11.5 自检与正确性

引擎自检第 8 组专门验证本节能力，关键做法是**独立重算**而不是"自己验自己"：

| 被测能力 | 独立验证方式 |
|---|---|
| SM2 加密 | 用固定随机数 k 自己重算 `C1 = kG`、`C2 = M ⊕ KDF(x2‖y2)`、`C3 = SM3(x2‖M‖y2)`，与原实现逐字节比对 |
| SM2 公钥 | 校验 C1 是否满足曲线方程 `y² ≡ x³ + ax + b (mod p)` |
| RSA 签名 | 手工构造 EMSA-PKCS1-v1_5 后做模幂，与签名的 `modpow(sig, e, n)` 比对 |
| 压缩 | 4 种算法的魔数与往返一致性 + 算法标错时的自动回退 |
| 编码链 | 8 种组合往返 + **顺序写错必须失败** |

---

## 十二、Tab 10「一键解密」怎么用（贴密文直接出明文）

前面九个页签是"零件"，这一页是**把零件串成流水线**：你只提供密文，工具自己判断这是什么、
用什么算法、密钥从哪来，然后尝试解密，最后给出**明文 + 解密逻辑说明 + 判定依据**。

### 12.1 怎么用

1. 打开 **Tab 10「一键解密」**，把密文粘进输入框（**任意形态都可以**，
   常见形态见 12.2；形态不典型时工具会自动抽出密文段兜底进解密矩阵）；
2. **可选但强烈建议**：点「选择目录」指定目标前端 JS/jar 所在目录 —— 工具会从里面
   自动挖出伪装常量与派生链（不指定的话，也可以把源码片段和密文**一起粘进输入框**）；
3. 点「一键分析并解密」；
4. 看结果：明文（可一键格式化 JSON / 复制）、解密逻辑说明、判定依据、尝试记录，
   以及**「用到的加解密逻辑」**——编号与 Tab 11 的逻辑 ID 一一对应
   （如 `kdf.aes-cbc-then-sha256-slice`、`hmac.hmac-sm3`），点一下就跳到 Tab 11
   查看这条逻辑的用途与适用场景；
5. 想继续调参复现，点「密钥/参数填入工作台」——**自动匹配**算法、模式、填充、编码，
   连密钥/IV 的解释方式（HEX/UTF-8/Base64）都会按「字节数正好匹配」一起带过去，
   各项设置保证一致。

### 12.2 支持的密文形态

| 形态 | 长什么样 | 处理方式 |
|---|---|---|
| 整段 HTTP 报文 | 带请求行/头 | 自动丢弃头部只取 body |
| 裸 Base64 | 规整 Base64 串（可带 `=`） | 直接解码后试解 |
| 裸 Base64URL | 含 `-` `_`、或去掉了 `=` 填充 | 按 Base64URL 解码（自动补回填充） |
| 裸 HEX | 纯十六进制串 | **优先于 Base64 判**（纯 hex 也满足 base64 字符集，先判 hex 才不会被解成乱码） |
| URL 编码 | 满屏 `%XX` 转义 | 先 unquote，拿到里面的 Base64/HEX 文本，再按该编码解码 |
| 种子前置 | 32 位 hex + Base64 密文 | 前 32 位当种子，走派生链（如 `SHA256(种子)[0:16]`） |
| **JSON 混合信封** | `{"encKey": <包裹密钥>, "iv": ..., "data": <对称密文>}` | 认成"信封"：先用材料里的**私钥**解封包裹密钥，再用解出的密钥解 `data`。字段名自动识别（`encKey`/`sked`/`key` ↔ `data`/`ed`/`cipher` ↔ `iv`/`nonce`） |
| 混杂文本 | 密文与源码/日志粘在一起 | 自动抽出其中的密文段（先找 ≥120 字符的长串；找不到再放宽到 ≥44 字符，并按「解码后不可打印 + 分组对齐」过滤掉源码里的长标识符），同时从整段里挖常量 |
| **形态不典型** | 以上都对不上 | **兜底**：抽出最长 token 按 Base64/HEX 解码，直接进解密矩阵试一轮（由明文打分裁决，不会把乱码当成功） |

### 12.3 它到底做了什么（六步）

```
① 形态识别      判断报文结构，切出 密文 / 前置种子 / mac
② 常量获取      粘贴文本 + 目标目录/文件 + 本地知识库 → 伪装常量、定长切片、派生链
③ 字段解析      取报文里前置的随机种子、按定长切片拿 AES 常量与 IV
④ 配方编排      两种路线：
                 · 对称类：种子 → 密钥派生（含「取中间 16 字节」）→ 对称解密 → 压缩还原 → mac 校验
                 · 信封类：私钥 → 解封包裹密钥 → 对称解密 → 压缩还原
                 「解不开的编码链」先剪掉（不铺进矩阵），避免组合爆炸把正确配方挤出去
⑤ 逐个试解      每个候选方案都跑一遍，明文按 UTF-8 / **字符级**可打印比例 / JSON / 业务字段名打分
⑥ 输出结论      明文 + 逐步解密逻辑 + 判定依据 + 全部尝试记录（失败的也给可读原因）
```

### 12.4 可信度怎么理解

| 结论 | 含义 |
|---|---|
| **外层 mac 校验通过** | 最硬的证据：用派生出的 master 重算 HMAC，与报文里的 mac **完全一致** → 判定确信（可信度 1.00） |
| mac 算法不写死 | 按 mac 的字节长度缩小范围（32 字节→HMAC-SM3 / HMAC-SHA256、20→SHA1、16→MD5）再逐个试。`HMAC-SM3` 与 `HMAC-SHA256` 都是 32 字节（Base64 后同为 44 字符），长度上分不出来，只能都试——对上了才知道是哪个 |
| 可信度 0.6~0.95 | 没 mac 可验，但明文是合法 UTF-8 + 合法 JSON + 命中业务字段名（`reqHead`/`txCd`/`chnlNo`…） |
| 未解密成功 | 列出每套方案的失败原因（填充校验不过 / 不是合法 UTF-8 / 密文长度异常…），不会把乱码当成功 |
| 缺少密钥材料 | 明确提示「需要目标前端 JS/jar 或直接给出常量」，**不瞎猜** |

> **常量不预置**：本工具不内置任何目标的密钥。常量只来自「你粘贴的文本」「你指定的目标目录」
> 或本地知识库（`crypto_knowledge.json`）里你已经学过的规则。

### 12.5 命令行等价用法

```bash
# 一键解密（推荐带 --target-dir，工具自己挖常量）
%PY% cli_crypto.py --auto-solve --file 密文.txt --target-dir <目标前端目录>
%PY% cli_crypto.py --auto-solve --text "<密文或整段报文>" --json

# 也可以单独跑这个模块
%PY% cipher_auto_solver.py --file 密文.txt --target-dir <目录>
%PY% cipher_auto_solver.py --selftest        # 自检（46 项：形态/材料/端到端/mac/负向）
```

### 12.6 一个真实例子（种子前置 + 常量派生）

```
形态：种子前置（32 字符随机种子 + Base64 密文）
材料来源：目标里的 67 字符常量（data-uri）→ AES 常量 / IV 两组定长切片
③ 密钥派生：aes-cbc-then-sha256-slice
   · AES128-CBC/PKCS7（密钥 16 字节、IV 16 字节）→ 48 字节
   · SHA256 → 32 字节
   · slice [16:32] → 16 字节
   · 正文密钥 = 派生结果
④ 正文解密：SM4-ECB / PKCS7，密文按 base64 解码
结论：解密成功，且明文打分通过（JSON 结构 + 业务字段名命中）
```

---

### 12.7 配套：密文样本集（14 个，全部可一键解出）

工具目录下带了两支脚本，用来**生成**和**验证**一整套多样化的密文样本——每一条都能被一键解密独立解出：

```
resources/tools/make_cipher_samples.py     生成样本集（密文 + 目标源码片段 + 标准答案）
resources/tools/verify_cipher_samples.py   用一键解密逐个跑，并生成分析报告
```

样本集覆盖面（每个样本目录里都有 `README.md` 写清「密文特征 / 分析思路」，
根目录的 `密文样本集-分析与解密报告.md` 写清「工具实际执行的解密过程 / 明文 / 依据」）：

| 维度 | 覆盖 |
|---|---|
| 分组算法 | SM4、AES-128、AES-192、AES-256、DES、3DES、RC4 |
| 工作模式 | ECB、CBC、CTR、GCM（含 AAD）、STREAM |
| 填充方式 | PKCS7、ZeroPadding、无填充 |
| 编码 / 压缩 | Base64、Base64URL、HEX、双层 Base64、URL 编码、`hex(base64)` 多级链；gzip、zlib |
| 报文形态 | 整段 HTTP / 裸 Base64 / 裸 Base64URL / 裸 HEX / URL / 种子前置 / JSON 混合信封 |
| 非对称信封 | RSA-2048 + OAEP-SHA256 包裹密钥；SM2 + C1C3C2 包裹密钥（`ed`/`sked` 字段写法） |

```bash
%PY% %TOOLS%\make_cipher_samples.py --out <输出目录>
%PY% %TOOLS%\verify_cipher_samples.py --dir <输出目录>
```

> 信封样本的 `server.js` 里放了私钥——那是为了让样本「可自证可解」。
> 真实场景中私钥在服务端；工具**只从你给的材料里挖私钥**，挖不到就明确告诉你缺什么。

## 十三、「加密逻辑总览」怎么用（逻辑清单 · 特征反查 · 按逻辑试算）

前面十个页签是"动手"的，这一页是"**先看清楚再动手**"：
它把工具里**全部加解密逻辑**（96 条原子逻辑 + 22 条端到端组合配方 + 20 条密文特征规则）
集中到一个页面，做到**一眼可见、清晰可查、可直接试算**。

数据源是 `python/crypto_logic_registry.py` —— 描述文字全部来自后端，**与代码同源**，
改了引擎重跑 `--export-md` 就能刷新配套文档（见 `加解密逻辑总览.md` 附录 A）。

### 13.1 三个区块

| 区块 | 干什么 |
|---|---|
| ① 按密文特征反查 | 贴密文 → 工具量出特征（是否纯 HEX / Base64URL / JSON 信封 / %XX / 末尾 44 字符 mac / 解码后是否压缩数据）→ 列出命中的特征规则与**建议试解顺序**，可一键交给一键解密 |
| ② 全部逻辑清单 | 左侧 17 个分类（带计数），右侧逻辑卡片（名称 / 用途 / 适用场景 / 参数 / 关联 / 支持状态 / 对应样本 / CLI 复现命令），支持关键词搜索 |
| ③ 按逻辑试算 | 选中任一条逻辑 → 填密文/明文 + 密钥/IV/方向/附加参数 → 「试算」，直接出结果与过程说明 |

### 13.2 支持状态标记

| 标记 | 含义 |
|---|---|
| ✅ 已支持 | 工具完整实现，直接可用 |
| ⚠️ 部分支持 | 能做，但有条件（如 AES-192 少见、RC4 属弱算法、raw 编码仅文件模式） |
| ❌ 未支持 | 工具做不到，**并且写清了缺什么、该怎么补**（如白盒 SM4、密钥协商、国密 TLS、so 内派生） |

### 13.3 命令行等价用法

```bash
%PY% crypto_logic_registry.py --list                       # 全量清单
%PY% crypto_logic_registry.py --list --cat envelope        # 只看混合信封
%PY% crypto_logic_registry.py --list --q 安全键盘 --json   # 关键词搜
%PY% crypto_logic_registry.py --features                   # 20 条密文特征规则表
%PY% crypto_logic_registry.py --match --file 密文.txt --json   # 贴密文反查
%PY% crypto_logic_registry.py --apply algo.sm4 --text 明文 --key 0011... --op enc
%PY% crypto_logic_registry.py --apply kdf.digest-sha256 --seed 5f2c... --slice 0:16
%PY% crypto_logic_registry.py --apply envelope.sm2 --text <cipher_hex> \
        --private-key <hex> --wrapped-key <hex> --op dec
%PY% crypto_logic_registry.py --export-md --out 加密逻辑清单.md
%PY% crypto_logic_registry.py --selftest                   # 44 项自检
```

### 13.4 与其它入口的分工

```
Tab 11 本页        「知道有什么」—— 逻辑目录 + 特征反查 + 单条试算
Tab 10 一键解密     「自动试出来」—— 贴密文 → 分析 → 试解 → 明文 + 依据
Tab 1/4/5          「手工精细控制」—— 拼参数 / 挖材料 / 跨文件配方
```

配套阅读：根目录《加解密逻辑总览.md》—— 四层模型、14 个样本归纳、
常见组合与工具对照、密文特征反查表、未支持清单。

---

## 十四、命令速查（CLI）

进到脚本目录后用内置解释器执行：

```bash
cd "%APP%\resources\python"
set PY=%APP%\resources\python-embed\python.exe
```

### 模块1：多算法多模式加解密（v2）

```bash
REM ① 自检（首次必跑，验证密码引擎全部 6 组用例）
%PY% cli_crypto.py --selftest

REM ② 查看引擎能力矩阵（算法 / 模式 / 填充 / 编码 / 哪些组合有完整性校验）
%PY% cli_crypto.py --spec --json
```

**② 实测输出（节选，已折叠长字段）**：
```json
{
  "version": 2,
  "algorithms": [
    {"id": "sm4",    "display": "SM4",     "key_sizes": [16], "key_hex_lens": [32],
     "default_key_bytes": 16, "key_hint": "16 字节（32 位 HEX）", "block": 16, "is_stream": false,
     "modes": ["ecb","cbc","cfb","ofb","ctr","gcm"],
     "implemented_in": "gmssl 分组原语 + 本模块模式组装"},
    {"id": "aes192", "display": "AES-192", "key_sizes": [24], "key_hex_lens": [48], "block": 16, "modes": ["ecb","cbc","cfb","ofb","ctr","gcm"]},
    {"id": "des3",   "display": "3DES",    "key_sizes": [24,16], "key_hex_lens": [48,32], "block": 8, "modes": ["ecb","cbc","cfb","ofb","ctr"]},
    {"id": "rc4",    "display": "RC4",     "key_sizes": [], "key_hex_lens": [], "is_stream": true, "modes": ["stream"]}
  ],
  "modes": [{"id": "cbc", "display": "CBC（密码分组链接，需要 IV）"}, {"id": "gcm", "display": "GCM（认证加密 AEAD，产出 16 字节认证标签）"}],
  "paddings": [{"id": "pkcs7", "display": "PKCS#7"}, {"id": "zero", "display": "ZeroPadding（零填充）"}],
  "encodings": [{"id": "base64", "display": "Base64"}, {"id": "hex", "display": "Hex"}, {"id": "raw", "display": "Raw（原始二进制，仅适合文件模式）"}],
  "stream_like_modes": ["cfb","ofb","ctr","gcm","stream"],
  "block_modes": ["ecb","cbc"],
  "families": {"sm4": "SM4（国密）", "aes": "AES（高级加密标准）", "des": "DES（数据加密标准）", "des3": "3DES（三重 DES）", "rc4": "RC4（流密码）"}
}
```
> GUI 的下拉框就是读这个输出渲染的，**引擎加算法，界面自动跟着加**。
> 完整字段（含每个算法的 `modes_display` 中文说明、`note` 备注）可用 `--spec --json` 原样查看。

```bash
REM ③ SM4-CBC 加密 hello（与旧版命令完全兼容）
%PY% cli_crypto.py --alg sm4 --op enc --mode cbc --padding pkcs7 ^
    --key 0123456789abcdeffedcba9876543210 ^
    --iv  00000000000000000000000000000000 ^
    --text "hello" --encoding base64
```
**实测输出**：`2wZpvYQeVFDsFlLs1CttYA==` （不带 `--mode/--padding` 时默认也是 CBC+PKCS7，旧脚本无需改动）

```bash
REM ④ SM4-ECB + Zero 填充加密（老系统常见组合）
%PY% cli_crypto.py --alg sm4 --op enc --mode ecb --padding zero ^
    --key 0123456789abcdeffedcba9876543210 ^
    --text "hello world" --encoding base64
```
**实测输出**：`RzFLNQQYCfWdX1o0NeuInQ==`（ECB 不需要 `--iv`，传了会报错提示）

```bash
REM ⑤ 解密回去（同参数切 --op dec）
%PY% cli_crypto.py --alg sm4 --op dec --mode ecb --padding zero ^
    --key 0123456789abcdeffedcba9876543210 ^
    --text "RzFLNQQYCfWdX1o0NeuInQ==" --encoding base64
```
**实测输出**：
```
[注意] ZeroPadding（零填充）不携带任何填充格式约束，无法据此判断密钥是否正确：
密钥错误时会静默产出乱码。如需可靠验证，建议改用 PKCS#7 填充，或使用 GCM 认证加密。
hello world
```

```bash
REM ⑥ AES-256-GCM 认证加密（自带防篡改，密钥/密文错一字节就报错）
%PY% cli_crypto.py --alg aes256 --op enc --mode gcm --padding none ^
    --key 00112233445566778899aabbccddeeff00112233445566778899aabbccddeeff ^
    --iv  000102030405060708090a0b --text "GCM authenticated!" --encoding base64
```

```bash
REM ⑦ 带附加数据 AAD 的 GCM
%PY% cli_crypto.py --alg aes256 --op enc --mode gcm --padding none ^
    --key <64位HEX> --iv <24位HEX> --aad "user=admin" --text "payload" --encoding base64
```

```bash
REM ⑧ 用错密钥会明确报错（不会给你乱码）—— PKCS7 填充有自校验
%PY% cli_crypto.py --alg sm4 --op dec --mode cbc --padding pkcs7 ^
    --key 00000000000000000000000000000000 ^
    --iv 00000000000000000000000000000000 --text "2wZpvYQeVFDsFlLs1CttYA==" --encoding base64
```
**实测输出**：
```
[参数错误] PKCS#7 去填充失败：填充字节非法，通常说明【密钥或 IV 错误】、或密文被截断/篡改
```

```bash
REM ⑨ 文件加解密（支持任意二进制）+ 明文直接输出裸文件
%PY% cli_crypto.py --alg aes256 --op enc --mode cbc --padding pkcs7 --key <64位HEX> --iv <32位HEX> ^
    --file D:\secret.zip --out D:\secret.zip.enc --encoding base64

REM 二进制密文就用 --encoding raw + --out（raw 必须指定输出文件）
%PY% cli_crypto.py --alg aes256 --op enc --mode gcm --padding none --key <64位HEX> --iv <24位HEX> ^
    --file D:\secret.zip --out D:\secret.bin --encoding raw

REM ⑩ JSON 输出（给脚本/自动化用）
%PY% cli_crypto.py --alg sm4 --op enc --mode ecb --padding zero --key <32位HEX> --text "hi" --json
```

**⑩ 实测输出**：
```json
{"status": "ok", "alg": "sm4", "alg_display": "SM4", "mode": "ecb", "padding": "zero",
 "op": "enc", "integrity_checked": false, "encoding": "base64",
 "cipher_len": 16, "plain_len": 2, "cipher": "N3P2/OMhRokk9NQ+TftEMQ=="}
```
> 注意 JSON 里密文字段名是 **`cipher`**（不是 ciphertext）；
> `integrity_checked` 告诉你这次操作有没有做完整性校验（`false` = 解密时需人工确认明文）。

### 模块1-b：算法 / 模式 / 填充 速查表

| `--alg` | 密钥长度 | 可用 `--mode` | 需要 `--iv` |
|---|---|---|---|
| `sm4` | 32 位 HEX（16 字节） | ecb/cbc/cfb/ofb/ctr/gcm | 除 ecb 外都要（GCM 为 12 字节） |
| `aes128` | 32 位 HEX | 同上 | 同上 |
| `aes192` | 48 位 HEX（24 字节） | 同上 | 同上 |
| `aes256` | 64 位 HEX（32 字节） | 同上 | 同上 |
| `des` | 16 位 HEX（8 字节） | ecb/cbc/cfb/ofb/ctr | 除 ecb 外都要（8 字节） |
| `des3` | 48 位 HEX（24 字节）或 32 位 HEX（16 字节，即 DES-EDE2） | ecb/cbc/cfb/ofb/ctr | 除 ecb 外都要（8 字节） |
| `rc4` | HEX，**5–256 字节**（10–512 位 HEX） | 只有流模式（不用填） | 不需要 |

| `--padding` | 说明 | 默认值 |
|---|---|---|
| `pkcs7` | 最通用，且**解密时能自动发现密钥错误** | ✅ 分组模式默认 |
| `zero` | 零填充，老系统常见；**不能发现密钥错误** | — |
| `iso7816` | 0x80 + 0x00 填充，能发现密钥错误 | — |
| `ansix923` | 0x00... + 长度字节，能发现密钥错误 | — |
| `none` | 不填充，要求数据长度是分组大小整数倍 | — |

| `--encoding` | 说明 |
|---|---|
| `base64` | 默认，密文/明文都是 Base64 文本 |
| `hex` | 十六进制文本 |
| `raw` | 裸二进制，**必须配合 `--out` 写文件** |

### 模块1-c：本工具的正确性是怎么验证的

**你在安装目录里就能自证的两层**（推荐首次使用时都跑一次）：

```bash
REM 第 1 层：引擎自检（国标向量 + 与 pycryptodome 原生实现对照 + 820 组往返 + 错误密钥检出）
%PY% cli_crypto.py --selftest

REM 第 2 层：扫描器自检（含与引擎能力矩阵的一致性比对、以及「识别即可复现」回归）
%PY% file_crypto_scanner.py --selftest

REM 附：MITM 插件自检（加解密往返 + 错误密钥检出）
%PY% mitm_crypto.py --selftest
```

**第 3 层：跨实现互操作**（开发期校验，需在有源码的项目目录、且本机有 node.exe）

本工具的密文与 OpenSSL / Node.js **逐字节比对**，并用 DES/SM4/AES 的公开标准向量
验证密码原语本身：

```bash
cd 源码目录\python
%PY% _dev_interop_check.py node.exe python.exe
```

**实测输出**：
```
 互操作对照：jiejieEAD  vs  Node.js / OpenSSL
  [ OK ] SM4-CBC/PKCS7        密文逐字节一致（32 字节）
  [ OK ] SM4-ECB/PKCS7        密文逐字节一致（16 字节）
  [ OK ] AES-128-CBC/PKCS7    密文逐字节一致（32 字节）
  [ OK ] AES-128-ECB/PKCS7    密文逐字节一致（32 字节）
  [ OK ] AES-192-CBC/PKCS7    密文逐字节一致（32 字节）
  [ OK ] AES-256-CBC/PKCS7    密文逐字节一致（32 字节）
  [ OK ] AES-128-CFB          密文逐字节一致（15 字节）
  [ OK ] AES-128-OFB          密文逐字节一致（15 字节）
  [ OK ] AES-128-CTR          密文逐字节一致（15 字节）
  [ OK ] AES-256-GCM          密文逐字节一致（34 字节）
  [ OK ] 3DES-CBC/PKCS7       密文逐字节一致（24 字节）

 标准测试向量：密码原语正确性（独立于任何第三方实现）
  [ OK ] [向量] DES-ECB       与标准向量一致 —— DES 经典公开示例向量
  [ OK ] [向量] SM4-ECB       与标准向量一致 —— GB/T 32907-2016 标准附录 A.1
  [ OK ] [向量] AES-128-ECB   与标准向量一致 —— FIPS-197 附录 C.1
  [ OK ] [向量] AES-256-ECB   与标准向量一致 —— FIPS-197 附录 C.3
 [PASS] 所有可比对的算法/模式输出与 OpenSSL 实现完全一致，
        通过公开标准向量的原语（DES/SM4/AES）亦完全正确。
```
> 意思是：**本工具产出的密文能被 OpenSSL / Node.js 解开，反之亦然**——
> 拿去对接真实业务系统是互通的，不是"自己算自己"。
>
> （单 DES 与 AES-GCM 中的 DES 项会被 Node 的 OpenSSL 3.x 跳过——单 DES 已移入
> legacy provider，属上游策略；因此单 DES 改用上面的公开标准向量独立验证。）

### 模块3：国密分析

```bash
REM ① SM3 摘要
%PY% gm_crypto_analyzer.py sm3hash --text "hello" --json
```
**实测输出**：
```json
{"status": "ok", "algorithm": "SM3", "source": "文本参数 --text", "length": 5,
 "sm3": "becbbfaae6548b8bf0cfcad5a27183cd1be6093b1cceccc303d9c61d0a645268"}
```

```bash
REM ② SM2 公钥安全审计（支持 PEM / X.509 证书 / 裸 HEX）
%PY% gm_crypto_analyzer.py sm2check --pubkey D:\pubkey.pem

REM ③ TLCP 国密流量解析（需要 pcap 抓包文件）
%PY% gm_crypto_analyzer.py tlcp --pcap D:\capture.pcap --port 443
```

### 模块2：mitmproxy 插件（命令行方式）

```bash
REM 用 mitmweb 加载插件：本机 8080 代理 + 8081 改包界面
%APP%\resources\python-embed\python.exe -c "from mitmproxy.tools.main import mitmweb; mitmweb(['-s', r'%APP%\resources\python\mitm_crypto.py', '-p', '8080', '--web-port', '8081'])"
```
> 命令行方式需要先通过环境变量注入配置（`JIEJIEEAD_MITM_CONFIG_FILE`），
> 一般直接用 GUI 的 Tab 2 更省事。

---

### 模块7：MCP 接入向导（Tab 7 的后端）

```bash
cd "%APP%\resources\python"
set PY=%APP%\resources\python-embed\python.exe

REM 体检：SDK / 依赖兼容 / 脚本 / 客户端登记情况
%PY% mcp_setup.py --status            REM 加 --json 输出机器可读（GUI 就是这么调的）

REM 装 / 修依赖（含 h11、httpcore 兼容 pin；GUI 按钮同一套逻辑）
%PY% mcp_setup.py --install

REM 生成 / 写入客户端配置
%PY% mcp_setup.py --config                       REM 打印可粘贴的 mcpServers JSON
%PY% mcp_setup.py --write workbuddy              REM 合并写入 ~/.workbuddy/mcp.json
%PY% mcp_setup.py --write claude                 REM 合并写入 Claude Desktop 配置
%PY% mcp_setup.py --write "D:\my\mcp.json"       REM 也可指定任意配置文件路径

REM 真实 stdio 握手自检：拉起服务器 → initialize → tools/list → 调用工具
%PY% mcp_setup.py --smoke

REM 列出暴露的工具 / 接入向导自检
%PY% mcp_setup.py --tools
%PY% mcp_setup.py --selftest
```

> 写入行为：**保留配置文件里已有的其它服务器**，只新增/覆盖 `jiejieead` 一项，
> 并在写入前生成 `<配置文件>.bak-jiejieead` 备份。写坏的配置可一键回滚。

`--status --json` 的关键字段（GUI 页签就是读这些）：

| 字段 | 含义 |
|---|---|
| `ready` | 是否已就绪可直接被客户端调用 |
| `blockers` | 未就绪的原因列表（界面上的红字提示） |
| `mcp_version` / `h11_version` / `httpcore_version` | 依赖版本，用于判断是否压到了兼容组合 |
| `pip_check_ok` | `pip check` 是否零破损（false 说明依赖打架） |
| `config.json` | 已按本机真实路径生成好的 `mcpServers` 配置文本 |
| `config.targets.workbuddy.registered` | 本服务器是否已登记进 WorkBuddy 配置 |
| `tools` | 暴露的工具清单（名称 / 说明 / 入参） |

---

## 十五、常见问题 FAQ

**Q1：双击 exe 没反应 / 提示找不到 Python？**
`resources` 文件夹必须和 `jiejieEAD.exe` **保持同级**。移动位置时两个要一起拷，别只拷 exe。

**Q2：提示缺 WebView2？**
Win11 自带。Win10 到微软官网下载安装 "Microsoft Edge WebView2 Runtime"（体积很小）。

**Q3：MITM 解密出来是乱码 / 报填充错误？**
KEY 或 IV 不对，或 Body 编码选错了。常见三种情况：
- 密文是 hex 文本 → 编码选 `Hex`
- 密文是 Base64 文本 → 编码选 `Base64`
- Body 直接就是二进制密文 → 编码选 `None`

**Q4：改包地址打不开？**
地址必须带 `?token=xxx`。点界面上的「复制改包地址」按钮取完整地址，别手打。

**Q5：为什么填全 0 的 KEY/IV 会告警？**
工具会检测弱密钥并提示。仅本地调试可用，别用于真实业务。

**Q6：能离线用吗？**
可以。除 MITM 模块需要访问被测目标外，加解密、国密分析与文件扫描全部本地离线完成。

**Q7：文件扫描扫出一堆密钥，用哪个？**
看置信度标签：优先用 `【高】` 的。判断依据是变量名上下文（如 `key=`/`iv=`）+ 长度特征（SM4=32 位 HEX，AES-256=64 位 HEX）+ 出现位置。`【中】` 一般是靠长度猜的候选，需要自己确认；`【低】` 只是可疑字符串。
另外注意**看算法标签配对**：`[高] 国密:SM4` 配上 32 位 HEX 才是一组。

**Q8：扫描报「未发现明显加密特征」？**
两种情况：① 文件确实没加密（纯文本/普通资源）；② 是**加壳或压缩后**的二进制。可以先勾上 **深度扫描** 试试钻包；如果还是空，说明密钥不是硬编码在文件里（可能在服务端下发或运行时拼装），需要走动态抓包（Tab 2 MITM）。

**Q9：扫描会卡住吗？**
不会。默认只读前 8MB（`--max-bytes`），超大文件也不会撑爆内存。要扫全量可以手动调大这个值。

**Q10：扫 APK 能扫到什么？**
会钻进 APK（本质是 ZIP）内部，逐个扫 `assets/`、`classes.dex` 伴生的 JS/H5 资源、`res/raw` 等文本文件，把里边的密钥和算法捞出来。原生 so 库只能给出「疑似二进制 + 加密库指纹」，不做反汇编。

**Q11：解密没报错，但结果是乱码，怎么回事？**
先看你的模式/填充组合。`ECB`/`CBC` + `Zero`/`None`，以及 `CFB`/`OFB`/`CTR`/`RC4`
这些组合**没有完整性校验**，密钥错了也会"成功"解出一堆合法字节。换用 `PKCS7` +
`CBC`（或 `GCM`）就能让工具在你密钥填错时立刻报错。选到无校验组合时，界面会弹橙色
提示条并在解密前二次确认，就是为了防止这种误判。

**Q12：为什么我拿 `CBC` 去解 `ECB` 的密文也能跑出结果？**
分组密码的模式是"算法的一部分"，不匹配时通常仍能完成运算，只是结果没有意义。
所以**光有密钥不够，模式/填充也必须对**。这也是 Tab 4 扫描器要把
`--mode` / `--padding` 一起识别出来并一键填入的原因。

**Q13：`--iv` 什么时候可以不给？**
`ECB` 模式不需要 IV；`RC4` 不需要 IV（它是流密码，没有分组模式）。
其余模式（CBC/CFB/OFB/CTR/GCM）都必须给。传了不该传的 IV 会明确报错，
不会静默忽略。

**Q14：支持 GCM 认证加密吗？AAD 怎么用？**
支持。`--mode gcm`，注意 IV 是 **12 字节（24 位 HEX）**，密文末尾会带
**16 字节认证标签**（解密时自动校验）。带附加认证数据用 `--aad "..."`。
密钥、IV、AAD、密文任一处被改一字节，解密都会报"认证失败"。

**Q15：怎么确认这工具算出来的密文是对的？**
跑三层验证（见「模块1-c」小节）：引擎自检、扫描器自检、与 Node.js/OpenSSL 的跨实现
逐字节对照 + DES/SM4/AES 公开标准向量。三层全绿，说明本工具与真实世界的
OpenSSL 实现完全互通。

**Q16：MCP 页签显示"未就绪"，怎么办？**
看页签里的红字（就是后端 `blockers`）。三种常见情况：
① 未装 mcp SDK → 点「一键安装 / 修复依赖」；
② 依赖存在冲突（`pip check` 不通）→ 同样点「一键安装 / 修复依赖」，它会把
h11 / httpcore 压回兼容版本；
③ 缺少脚本（服务器脚本或 crypto_core.py 不在）→ 包体不完整，重新解压/安装。

**Q17：装完 MCP 之后 MITM 页签起不来了？**
这是依赖打架：`mcp` 会把 `h11` 升到 0.16，而 `mitmproxy 11.0.2` 要求 `h11<=0.14.0`。
点一次「一键安装 / 修复依赖」即可（等价命令
`python.exe -m pip install "h11==0.14.0" "httpcore==1.0.7"`），
压完后 `pip check` 应为 `No broken requirements found`。

**Q18：已经"写入 WorkBuddy"了，客户端还是看不到工具？**
写入只是改配置文件，**不会热加载**。需要重启客户端；WorkBuddy 还要在连接器管理页
右上角「自定义连接器」里对新增的 `jiejieead` 服务器点 **「信任」**。
可点页签里的「重新体检」，`config.targets.workbuddy.registered = true` 就说明配置写进去了。

**Q19：MCP 写入会不会把我原有配置搞乱？**
不会。写入是**合并**：只新增/覆盖 `jiejieead` 一项，其它服务器原样保留，
写入前还会生成 `<配置文件>.bak-jiejieead` 备份，搞坏了直接把这个备份改回原名即可。

**Q20：为什么接入配置里要带 `PYTHONIOENCODING=utf-8`？**
工具通过 stdio 返回的 JSON 里有中文（例如「PKCS#7 去填充失败：填充字节非法…」）。
部分 Windows 客户端按 GBK 解码 stdio 会把这些中文变成乱码，显式声明 UTF-8 最稳。
「生成配置 / 写入配置」产出的 JSON 已经自动带上这一项。

**Q21：包里有我自己的 API Key 吗？会被一起发出去吗？**
不会。发布版已清空所有运行期密钥：`ai_config.json` 的 `api_key` 为空、`enabled=false`，
`crypto_knowledge.json` 是空库。自己填的 Key 会被写进 `ai_config.json`——
**如果你要把包发给别人，发之前先删掉这个文件**，或从一开始就用包外路径
（`JIEJIEEAD_AI_CONFIG` 指向包外文件）。
另外打包脚本 `build-exe.ps1` 现在内置了「发布前安全检查」，发现包内有非空 Key
或非空知识库会**直接阻止打包**，从流程上兜住这一点。

**Q22：有没有自动化的密钥泄漏检查？**
有，`tools/preflight_secret_scan.py`（零依赖，纯标准库）：

```bash
python tools/preflight_secret_scan.py                     # 默认扫 src-tauri/resources
python tools/preflight_secret_scan.py --dir <要检查的目录>  # 扫任意目录
python tools/preflight_secret_scan.py --json              # JSON 输出，便于接 CI
python tools/preflight_secret_scan.py --self-test         # 自检扫描逻辑（8 个场景）
```

检查项：预置 `api_key`、非空知识库、运行期残留备份（`.bak-*` / `.orig` / `.removed-*`）、
常见 API Key 特征（`sk-…` / `AIza…` / `ghp_…` / `xox…` / `AKIA…`）、PEM 私钥材料；
模板里的占位符（`<...>` / `your_key` / `填你自己的`）只报 INFO 不报错，避免误杀。
退出码：`0` 干净、`1` 发现问题（禁止发布）。
注意：默认跳过第三方依赖目录（`site-packages`）——库里自带的测试固件（例如
Cryptodome 的自检 PEM、tornado 的 test.key）会产生大量噪声、反而掩盖真问题；
要连第三方一起扫加 `--include-site-packages`。

**Q23：`build-exe.ps1` 执行时报一堆「意外的标记 }」「参数列表中缺少参量」？**
那是 UTF-8 **BOM** 缺失导致的：Windows PowerShell 5.1 对无 BOM 的文件按 ANSI/GBK 解码，
脚本里的中文被解坏后会连带把引号、括号判成语法错误（表现为一堆 ParseError，
脚本一行都跑不到）。本版已给脚本补上 BOM，正常情况下不会再出现。
若手改脚本后又复现，用**带 BOM 的 UTF-8**重新保存即可。

**Q25：密钥长度一直提示"不合法"，但我是从代码里原样抄的？**
先看**密钥格式**。真实代码里的密钥常常是字符串字面量，比如
`var KEY = "4A7F2C1E9B3D5A08"` —— 这是 **16 个 ASCII 字符 = 16 字节**，
但按 HEX 解析只有 8 字节，AES-128 会直接报长度错。
把「密钥格式」切成 **UTF-8 字面量**即可；拿不准就先跑
`cli_crypto.py --keyinfo --key <你的密钥>`，它会同时给出三种解读的字节数。
工作台也会主动提示这类歧义。

**Q26：为什么我算的签名（mac）跟报文里对不上？**
按这三条排查：
① **是 HMAC 还是"拼接摘要"**？`HMAC-SM3(key, msg)` 与 `SM3(key + msg)` 完全不是一回事，
本工具 Tab 8 支持前者（HMAC-SM3 与 BouncyCastle 逐字节一致）；
② **覆盖范围对不对**？常见是「被保护的字段 + 密文」拼接后整体签名，
少拼一段、多拼一段都会变；注意是**拼接后的字符串**再取 UTF-8 字节，
不是先解码再拼；
③ **密钥是不是派生出来的**？外层签名用的往往不是正文密钥，而是
`SHA256(AES-CBC(种子))` 得到的 32 字节 master —— 用 Tab 8 的「密钥派生」把它算出来后
再喂给 HMAC。

**Q27：怎么快速判断一个陌生目标用的是什么？**
推荐顺序：**Tab 4 文件扫描分析**（丢一个 JS / jar 出来的文件进去）→ 拿扫描结论的
算法/模式/填充/密钥 → 若有"密钥像是算出来的"迹象，去 **Tab 8** 试派生预设 →
拿到的密钥回 **Tab 1** 解密 → 用 **Tab 8 的 HMAC** 反推签名，能对上就说明链路复现正确。
整条链路里，**编码（二次 Base64 / Hex）与字节区间**是最容易出错的两处。

**Q28：扫描结果里说"未发现明显加密特征"，但我知道它一定有加密？**
四个方向逐个试：
① 密钥可能**藏在伪装常量里**——看结果面板底部「伪装常量与密钥派生链」，有没有
`data:xxx;base64,`、长 base64 或长 HEX 串被解出来；
② 密钥可能是**算出来的**——同处会给出一条派生链与可执行命令，把它复制到 Tab 8 或命令行跑一遍；
③ 文件可能是**二进制/字节码**（`.class`/`.dex`/`.so`）——加 `--deep` 再扫一次，
它会先抽可打印串再分析（Java class 常量池的字符串就是这么拿到的）；
④ 加密可能在**压缩包内层**——用「文件夹整体分析」而不是单文件扫描，
它会递归进去（注意：归档类文件不再受 4MB 单文件上限限制，默认放宽到 64MB，
因为一个 6MB 的 jar 里真正有价值的常量可能只有几百 KB）。

**Q29：派生链的"置信度"怎么看？**
`0.8~0.95`：同一文件里同时找到了加密调用、摘要调用、取字节区间，且常量已绑定——基本可以直接用；
`0.5~0.7`：形态匹配但常量来自别处或区间不确定——照着命令试一次即可验证；
`0.3`：**仅凭字节码常量池共现推断**（`.class` 里没有可读顺序信息），
卡片上会标「字节码常量池共现推断」，建议先反编译确认顺序再动手。

**Q30：SM2 签名明明是对的，对方却说验签失败？**
三种常见原因：
① **没带公钥**：SM2 的签名要算 SM3 的 Z 值，而 Z 里含公钥分量。只给私钥时若实现不当，
Z 会按"空公钥"计算，对方用真实公钥必然验不过——本工具会自动从私钥推导公钥规避这一点；
② **签名格式**：`r||s` 的 128 位 HEX 与 DER 编码不通用，后者要勾选 `asn1`／加 `--asn1`；
③ **预处理不同**：对方若"先 SM3 再签裸摘要"，需要勾选「输入已是摘要」／加 `--prehashed`。

**Q31：SM2 解密/验签偶尔失败、报"KDF 输出全零"或"随机数越界"？**
这是 SM2 的固有概率（k 的取值有约 2⁻²⁵⁶ / 2⁻²⁵⁶ 的边界情况），**重试一次即可**。
另外提醒：`gmssl` 默认用非密码学的 `random` 模块生成 k，随机数一旦可预测私钥就可能被还原——
本工具已在调用期间强制替换为密码学安全的 `secrets`，你不需要额外处理。

**Q32：一键解密说"缺少密钥材料"，怎么办？**
这是**故意不猜**的结果：没有密钥/常量时硬试只会给你乱码。三条路任选——
① 点「选择目录」指到目标前端 JS 或客户端 jar/apk 所在目录（工具会自动挖出伪装常量与派生链）；
② 把密文和**含常量的源码片段一起粘进输入框**（工具会先挑出密文，再从整段里挖常量）；
③ 在 Tab 1/8/9 里手工解一次，点「学习此规则」把它存进本地知识库，之后同类目标直接命中。

**Q33：一键解密给出的结论可信吗？**
看两点：**① 有没有 mac 校验通过**——有就是硬证据（工具会把"报文 mac / 本地计算"两行都列出来供你核对）；
**② 没有 mac 时看明文特征**——合法 UTF-8 + 合法 JSON + 命中业务字段名（`reqHead`/`txCd` 等）。
工具**从不把乱码当成功**：UTF-8 或可打印比例不合格会直接判失败并说明原因。

**Q34：密文是一个 JSON 信封（里面既有"密钥"又有"密文"），一键解密能处理吗？**
能。识别为「JSON 混合信封」后，它走的是**解封**路线：先用材料里的私钥（RSA 或 SM2）
解开 `encKey`/`sked` 字段得到对称密钥，再用那把密钥解 `data`/`ed` 字段。
两点要注意：① 需要**私钥**——请把服务端解密代码或密钥文件所在目录一起给它；
② 解封出来的密钥长度必须是 16 / 24 / 32 字节，否则说明填充或算法不对，工具会直接排除
（**RSA PKCS#1 v1.5 解密没有完整性校验，用错填充解错也不报错**，只能靠长度筛）。

**Q35：为什么有的明文是中文，却能被正确判定为"不是乱码"？**
因为打分是按**字符**算可打印比例，不是按字节。UTF-8 里一个汉字占 3 字节、全在 0x20~0x7E 之外，
按字节算的话一篇纯中文明文可打印率只有七成，会被冤枉成乱码。现在先尝试 UTF-8 解码，
再用 `isprintable()` 按字符统计——中文、全角标点都算可打印。

**Q36：一键解密试了很多套方案都不对，怎么定位问题？**
看「尝试记录」里每条失败的原因，它们已经区分类别了：
① **密文长度不是分组的整数倍** → 编码或密文切分位置不对；
② **Base64/HEX 解码失败** → 编码链猜错；
③ **不是合法 UTF-8 / 可打印字符比例过低** → 密钥不对（或该目标不是这套体系）；
④ **解封出来的不是对称密钥长度** → 信封的填充/算法不匹配；
⑤ **mac 不一致** → 密钥对、但 mac 算法或覆盖范围不是它以为的那种。
另外「解密逻辑说明」里每一行都写了派生/解封的中间结果（长度 + 前缀），可以拿去和源码逐字节对照。

**Q24：为什么「MCP 接入」页签要单独写一个 `mcp_setup.py`，而不是直接改 JSON？**
因为「能不能被调用」不只是配置写对——还取决于 SDK 是否装好、依赖有没有打架、
服务器脚本在不在、stdio 编码对不对。`mcp_setup.py` 把这些做成可编程的体检，
GUI 与命令行走同一套逻辑，避免"界面显示成功、实际起不来"。

---

## 十六、支持矩阵（密码引擎 v4）

### 16.1 分组算法

| 算法 | `--alg` | 密钥长度 | 支持模式 | 分组大小 |
|---|---|---|---|---|
| SM4（国密） | `sm4` | 16 字节 | ECB/CBC/CFB/OFB/CTR/GCM | 16 |
| AES-128 | `aes128` | 16 字节 | ECB/CBC/CFB/OFB/CTR/GCM | 16 |
| AES-192 | `aes192` | 24 字节 | ECB/CBC/CFB/OFB/CTR/GCM | 16 |
| AES-256 | `aes256` | 32 字节 | ECB/CBC/CFB/OFB/CTR/GCM | 16 |
| DES | `des` | 8 字节 | ECB/CBC/CFB/OFB/CTR | 8 |
| 3DES | `des3` | 24 字节（或 16 字节 = DES-EDE2） | ECB/CBC/CFB/OFB/CTR | 8 |
| RC4（流密码） | `rc4` | 5–256 字节 | 流模式（无分组） | — |

### 16.2 填充方式

| 填充 | `--padding` | 有完整性校验 | 备注 |
|---|---|---|---|
| PKCS#7 | `pkcs7` | ✅ | 最常用；16 字节分组下与 PKCS#5 等价，且**总是补一整块** |
| ZeroPadding | `zero` | ❌ | 错误密钥会静默解出乱码，界面会主动提醒人工核对明文 |
| NoPadding | `none` | ❌ | 要求明文长度恰为分组整数倍，否则直接报错 |
| ISO/IEC 7816-4 | `iso7816` | ✅ | 首字节 0x80，其余补 0 |
| ANSI X9.23 | `ansix923` | ✅ | 填充字节全 0，末字节记录补位长度 |

### 16.3 模式与 IV

| 模式 | 需要 IV | 需要填充 | 有完整性校验 |
|---|---|---|---|
| `ecb` | ❌ | ✅ | 仅取决于填充 |
| `cbc` | ✅ 16/8 字节 | ✅ | 仅取决于填充 |
| `cfb` | ✅ 16/8 字节 | ❌ 流式 | ❌ |
| `ofb` | ✅ 16/8 字节 | ❌ 流式 | ❌ |
| `ctr` | ✅ 16/8 字节 | ❌ 流式 | ❌ |
| `gcm` | ✅ 12 或 16 字节 | ❌ | ✅ 认证标签 |

### 16.4 密钥材料与派生（v3 新增）

| 能力 | 取值 | 说明 |
|---|---|---|
| 密钥格式 | `hex` / `utf8` / `base64` / `auto` | 同一串文本按不同格式字节数不同，`--keyinfo` 可一次性看全 |
| 摘要 | `md5` / `sha1` / `sha256` / `sha512` / `sm3` | 输出可 HEX 或 Base64 |
| HMAC | `hmac-md5` / `hmac-sha1` / `hmac-sha256` / `hmac-sha512` / **`hmac-sm3`** | HMAC-SM3 与 BouncyCastle 逐字节一致 |
| 派生预设 | `digest-*` / `hmac-*` / `aes-cbc-then-sha256` / `aes-cbc-then-sha256-slice` / `sm4-cbc-then-sm3` | 种子 → 加密 → 摘要 → 取字节区间 |

### 16.5 非对称 / 压缩 / 编码链（v4 新增）

| 能力 | 取值 | 说明 |
|---|---|---|
| SM2 | `enc` / `dec` / `sign` / `verify` | 密文格式 `c1c3c2` / `c1c2c3`；`asn1` 支持 DER；`prehashed` 直接签摘要 |
| RSA | `enc` / `dec` / `sign` / `verify` | 填充 `pkcs1v15` / `oaep`；签名 `pkcs1v15` / `pss`；摘要 md5~sha512；长文本自动分段 |
| 压缩 | `zlib` / `gzip` / `deflate` / `deflate-raw` | 解压失败自动回退识别 |
| 编码链 | `base64` / `base64url` / `hex` / `url` + 四种压缩 | 任意组合、顺序可控，解码自动反序 |
| 混合信封 | `--recipient sm2|rsa` + 任意对称算法 | 公钥包对称密钥 + 对称加密数据 |

### 16.6 一键解密流水线（v3.3 新增）

| 环节 | 覆盖范围 |
|---|---|
| 形态识别 | 整段 HTTP / 裸 Base64 / 裸 Base64URL / 裸 HEX / URL 编码 / 种子前置 / **JSON 混合信封** / 混杂文本（自动抽密文段） |
| 常量来源 | 粘贴文本、目标目录或单文件（伪装 base64、定长切片、派生链）、本地知识库 —— **不预置任何目标密钥** |
| 派生 | 复用引擎 KDF（8 个预设，含「取中间 16 字节」） |
| 解码/还原 | Base64 / HEX / Base64URL / URL / zlib / gzip / deflate / deflate-raw |
| 试解组合 | 对称算法 × 模式 × PKCS7/Zero/None，逐套尝试（有上限，防止炸）；解不开的编码链先剪掉 |
| 非对称信封 | **RSA**（PKCS#1 v1.5 / OAEP × SHA-1/SHA-256）+ **SM2**（C1C3C2 / C1C2C3）解封包裹密钥；私钥来自材料里的 PEM 或多行源码字面量 |
| mac 校验 | 按 mac 字节长度自适应候选算法：32→HMAC-SM3 / HMAC-SHA256、20→SHA1、16→MD5 |
| 判定 | UTF-8 合法性 / 可打印比例 / JSON 结构 / 业务字段名命中；**能验 mac 就验 mac** |
| 输出 | 明文、逐步解密逻辑、判定依据、全部尝试记录与失败原因 |

> **能力矩阵是唯一事实来源**：以上内容由 `cli_crypto.py --spec --json` 动态产出，
> GUI 下拉框、MITM 配置校验、文件扫描器镜像表三处都从同一个引擎读取/比对。
> 想确认某个组合跑不跑得了，直接查 `--spec`；引擎自检 `--selftest` 共 7 组、
> 含与 pycryptodome 原生实现、BouncyCastle HMAC-SM3、公开标准向量以及银行
> 报文形态脱敏向量的逐字节对照。

---

## 十七、使用边界

本工具面向**红队护网、APP 渗透、国密密码测评**等**已获书面授权**的场景。
未经授权对他人系统进行渗透、破解属于违法行为。

- **只测试你拥有书面授权的目标**：渗透测试授权书、众测平台委托、自有资产；
- **不留痕、不越权、不下载业务数据**：验证到"能进出"即可，不批量拖库、不横向扩散；
- **密钥与学习记录不要随包分发**：`ai_config.json` / `crypto_knowledge.json` 属于运行期数据，
  发布前请跑一次 `tools/preflight_secret_scan.py` 自检（打包脚本已内置该检查）；
- **工具不联网**：除「AI 智能分析」需要你自己配置的在线 LLM 外，全部运算在本机完成，
  密钥、明文、密文都不会上传。
